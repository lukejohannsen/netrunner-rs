# Netrunner Workspace Roadmap

**The single source of truth for status.** `AGENTS.md` is the rules of engagement, `ARCHITECTURE.md` the shape of the engine. This file is the index: where each area stands, what is next, and which area roadmap under `docs/roadmap/` holds the record. **Open an area roadmap only when working in that area** — each is the full log of decisions and measurements for its phase, and none is needed to orient.

Code and docs address entries as `ROADMAP <phase> §<n>` — "Phase 2 §5", "Rules Audit T8", "Phase 1 §8 Stage 5", "Phase 1.75 §6". Those addresses are permanent and resolve through the table below; an area roadmap keeps its section numbers even when an item is compressed to a line.

**Current goal:** a solid single-player Netrunner, then network play. **"Solid" means a person can feel it**: bots with a style a deck chooses, a record that names the rung to try next, and a difficulty ladder they can climb (Phase 5; a *rating* is between people and a server's to keep, Phase 4 §5) — the bots were all built for strength, and strength alone is not something a person can climb. **Phase 2 §5's search research is parked behind that** (13 September 2026): from 4 to 12 September, 49 merges and 28 roadmap entries (§5 items 14–41) went to why a network or PUCT's Runner chair cannot beat the uniform search, six volume runs promoted nothing, and the last three experiments shipped at weight 0.0 and byte-identical. None of the three player-facing pieces needed a stronger bot; all three were wiring and data over code that existed. They come first now.

**Health (10 September 2026):** `cargo test --workspace` green (about 1,200 tests), `cargo clippy --workspace --all-targets` silent, both 256-seed sweeps clean under the rules-coverage gate, the card-conservation invariant and the fog gate, with **no stall tolerance** — every seating reaches `GameOver`. `ActionSpace::SIZE` 1646, `OBS_SIZE` 2262, `CARD_VOCAB` 192, `MAX_STEPS` 2,500, `DECISION_BUDGET` 256. **The shipped bots are complete and carry no data**: `Random`, `Heuristic` with its six `Personality` profiles, `Mcts` and `Puct` are pure code — `eval::Weights` constants compiled into `netrunner_bots`, no file read at run time. The one bot that consumes a file, `puct-onnx`, sits behind an off-by-default feature, and **no trained network has yet beaten the `Puct` search it would replace** as a whole: four volume runs, none promoted. **Its two halves have now come apart** (§5 item 22, 8 September 2026) — a checkpoint trained on this engine scores **0.617 priors-only**, over the 0.55 threshold and above baseline on both chairs, against **0.141 value-only**, and seating both gives 0.359. **The fifth run then seated the priors alone and found they do not compound** (§5 item 26): over 1,152 arena games the candidate is −0.8σ from parity. **The "seat trade" read off that run's chair split is withdrawn** (§5 item 32, 10 September 2026): a chair's null is the pool's Corp win rate at that configuration — measured **0.6823 / 0.3177** — not 0.500, so the ±0.22 chair deltas are +0.026 and −0.050 and the candidate is at parity on both chairs. The per-chair promotion floor built on that reading (§5 item 28) was centred on 0.500 and unreachable, and **§5 item 33 re-centres it on a null leg measured per incumbent per arena shape** (`--arena-null`), which restores the gate's ability to promote without changing a single recorded verdict; the `(chair, outcome)` sample weight (§5 item 29) is a small real gain, cb05 being the one leg at or above its null on both chairs. **§5 item 34 then closed the question those nulls left open** (10 September 2026): the pool's Corp share is a function of *search budget* — 0.549 at 32 simulations, 0.586 at 128, **0.703 at 512** with the uniform evaluator and no network anywhere in the process — so seating a policy head never moved the pool; playing like a deeper search did. The Corp chair converts that budget and the Runner chair does not, and `mcts@128` beats `puct@128` on the Runner chair by 0.077 (2.95σ) while losing the Corp chair by 0.090 — making **PUCT's Runner chair the binding constraint on every chair number recorded here**. **§5 item 35 then found what that chair is short of, and it is not depth** (10 September 2026): MCTS's whole lead is its root-parallel tree count — 4 × 32, read off the core count — and at one tree the two searches are within 0.003. Four *different* samples of the hidden state are worth **+0.125** on that chair at identical depth, four copies of one sample only +0.031 (n.s.), and depth past 64 nothing at all, while the Corp chair converts depth (+0.208 over 16 → 128) and not samples. The same knob on PUCT is **−0.102**, landing on `puct@32`'s number. **§5 item 36 then found why, and the answer is structural** (10 September 2026): `evaluate_state_with` is bit-identical across all 16 determinizations in every one of 384 Corp positions and unanimous on the root argmax for the Runner too — PUCT at `samples` 4 averages four identical numbers — while at `mcts`'s 16-ply leaf the sample is worth **+0.070 (z = +7.74)** of argmax movement on the Runner chair against **+0.007 (n.s.)** on the Corp's. **§5 items 37 and 38 then fixed the prerequisite and closed the question** (11 September 2026): every unrezzed ICE in a sample was a `Barrier` with no subroutines and nothing ever repaired it, so item 36's 16-ply sensitivity was sampled HQ and R&D and never the ICE; and the leaf term that finally reads the sampled ICE moves the depth-0 column (+0.032, z = +4.49) while buying **nothing** in games (+0.016/+0.010 on two seeds, below the 0.026–0.047 band), shipping at weight 0.0. The retired runs and their checkpoints are under `data/runs/`; the corpora are deleted.

## Where things stand

| Area | Status | Record |
|---|---|---|
| **Phase 1** — Single-player completeness | **Done.** §5 Format Support closed: mechanisms enforced, tables a documented seed. Saved decks, legible view, the rules gaps, *System Gateway* 77/77, *Elevation* 82/82 with all 28 published decks (192 matchups). | [phase-1-single-player.md](docs/roadmap/phase-1-single-player.md) |
| **Phase 1.5** — Session unification | Done. One `Session`, one `MAX_STEPS`, two seat kinds. | same file, tail |
| **Rules Audit** and the card fidelity audits | Closed. Twelve Tier-1 rules, Tier 2, six hazard classes; System Gateway 11 and Core Set 4 deviations fixed. **One masking finding reopened it briefly (17 September 2026): the Corp was asked to pay for a card it could not see.** `PendingInteractiveTrigger` masked the accessed card by the whole-breach rule, but Snare! and Byte! both refuse to fire on Archives, so the Corp's question was *always* put about a card its own `ClientView` blanked — while `legal_actions_for` handed it `PayAccessTrigger { card_id }` with the real identity. The mask was withholding the card from the panel, not from the player. Widened to name the card to the decider. Random-vs-random byte-identical; heuristic Corp `PayAccessTrigger` 8 → 2 and `DeclineAccessTrigger` 6 → 12 (same 14 decisions, better informed), Corp wins flat at 184/192. The sweep's existing no-leak check missed it because it only covered cards on the table; its complement now checks that every card an action names is one the view shows. **Reopened as a design backlog (19 September 2026):** a comparison with jinteki.net ([docs/jinteki-comparison.md](docs/jinteki-comparison.md)) names six engine gaps for the next set, led by a continuous-effect layer that would replace a dozen one-off fields. **A second pass (20 September 2026) audited that list and re-ordered it to ten:** the six held, but the largest gap had been understated — which cards hear an event is decided per event in `rules/dispatcher.rs`, and an event an effect produces is never dispatched at all, so a new card can need a Rust edit with no new mechanic in it; the continuous-effect shape was too narrow (no target, numbers only — five of jinteki's twelve most-used kinds are not numbers); and turn-history queries, payment sources and a numeric decision were missed. **A report from play reopened it again (20 September 2026): advancing a card and placing a counter on it were one event.** Seamless Launch offering ice looked wrong and is right — CR 1.18.2 says placing is not advancing, 1.18.3's "can be advanced" restricts only advancing, and the rules' own example places counters on ice — but `Effect::AddAdvancementTokens` emitted `GameEvent::CardAdvanced`, the event the basic action emits. The first reading, that Built to Last was being paid for a placement, was **wrong and is corrected in the entry**: an effect's events are returned, never dispatched, so the trigger never saw one. Measured both ways — 192 random-vs-random games and 96 heuristic games on `brick_stack` are byte-identical across the change, 89 of 514 and 17 of 166 advance events reclassified, the identity's trigger count unmoved. Split anyway, because the plumbing was the only thing holding two rules apart. The effect is now `PlaceAdvancementCounters`, and Sericulture Expansion — which prints no "you can advance" clause and filtered on one — was refusing legal targets. **Backlog item 1 taken up (20 September 2026) — one event queue, one audience rule, one checkpoint — and its first PR is the measurement**: `scripts/coverage_identical.py` pins each ref's binary by sha and plays one full pass of the pool four ways (random and heuristic, by view and by index), so "byte-identical" is a command with an exit status rather than an md5 taken by hand; it reproduces #85's recorded deltas exactly. **Then the audience rule itself** (`feat/one-audience-rule`): `rules::listeners` — the subject of an event always hears it, every other card must be active, the active player first — replaces the dispatcher's per-event `match`, with `TriggeredEffect::subject` (`This`/`Any`) on the 128 card entries that needed the word. Run in shadow beside the old audiences over both 256-seed sweeps, the two agreed on every card in the pool except where the old table was wrong: **a faceup Orbital Superiority did its damage when a different agenda was scored** (live on `main`, six times in 768 games), and the active player's triggers did not come first on a steal or a rez. Not byte-identical, by construction — a side now orders its whole plan rather than one hand-written step of it (`TriggerOrderPending` 466 → 473 over 192 random games, outcomes inside the band) — which corrects the stage plan. **Then the queue, as a check rather than a rewrite** (`feat/one-event-queue`): in every debug build an action fails if its record holds an event a card could hear that was never dispatched (`dispatcher::audit`), which on its first run found that an install made by a card's text (Brân 1.0) was never heard by Engineering the Future; byte-identical four ways. **Then the checkpoint, which turned out to be two rules corrections** (`feat/one-checkpoint`): `rules::checkpoint` runs CR 10.3's standing checks *before* anything reacts and again at the end of every action, replacing ten hand calls. A steal that reaches the threshold now wins before the identity reacts to it (Personal Evolution used to flatline the winner), and the ◆ rule is about *active* cards, so a second facedown Spin Doctor no longer destroys the first (rezzed 120 → 151, trashed 23 → 58 over 192 heuristic games; outcomes inside the band). **Last, the `Trigger` variants that were one event and a filter** (`feat/triggers-take-a-filter`), which closes the item: `TriggeredEffect::when` is an `EventFilter` over the card or the server a moment is about, evaluated in the scan, so `OnSuccessfulRunOnHq`/`OnRnD`/`OnCentralServer`, `OnTransactionPlayed` and `OnVirusInstalled` are gone (`Trigger` 34 → 29) and the next "whenever you install a piece of hardware" is a card file. `EffectRequirement::TriggeringCardMatches` went into the same field, and Cookbook's "place 1 virus counter on **it**" — the last audience rule in the dispatcher — is `acts_on_subject` on the card. Identical but for the renamed `triggers_fired` keys, four reports of four. **Backlog item 2, the continuous-effect layer, taken up the same day in six stages, and its first is built** (`feat/continuous-effects-are-declared`): a card declares what it does while it is active — `CardDefinition::continuous`, a kind with its payload, a `Scope` read from the card that prints it, and a `while` — and `rules::continuous` is the one scan that reads it, asking `rules::active` which cards are active (a sentence `listeners` and `checkpoint` had each written out). Seven one-off fields, two `StrengthModifier` variants and six board scans went into it. In shadow beside the old scans over both 256-seed sweeps: no disagreement; with the old code deleted: identical, four reports of four. Then the two rules corrections the fields had hidden, measured apart: Mahkota Langit Grid taxed an asset accessed out of HQ when it sat in HQ's root (heuristic identical; random steps 68,364 → 68,212, end reasons unmoved), and the second DZMZ Optimizer in the nine sample decks that play two did nothing (random end reasons 93 / 85 / 14 → 94 / 84 / 14, heuristic one game's worth). **Then hand size** (`feat/hand-size-is-derived`): the one standing number still stored — folded in at setup, at an install and at a score, never taken out — is `ContinuousKind::HandSize` on its three cards, and `Effect::GainMaxHandSize` is gone. In shadow over both 256-seed sweeps a trashed T400 Memory Diamond was still paying in five games of 768, and every bot sample of a game with one of the three cards had the wrong limit, because `determinize` zeroed the stored bonus; one pass of the pool moves nothing but the deleted keys. **Then the effects with a duration** (`feat/lingering-effects`): `GameState::lingering`, each entry resolved to a flat number when it is made and holding for as long as the *state* says its duration is running — asked at every read, so the sweep in `rules::checkpoint` is garbage collection and no call site can forget one, which is what T8 was. The three `*_strength_buff` fields, `effective_strength()` and five reset sites are gone, and Leech's "for the remainder of this encounter", which was written into the run's copy of the ice and lasted the run, ends with the encounter. In shadow over both 256-seed sweeps: no disagreement; with the old code deleted: identical, four reports of four. **Then the view** (`feat/the-view-shows-the-strength-the-engine-uses`): a rig card's shown strength was the stored half alone, so Echelon, Rising Tide and a GAMEDRAGON™ Pro's host read short of the number the break contest used — 80,082 maskings of a rig card over both 256-seed sweeps. `mask_state_for_player` takes the registry (the reason it did not was wrong: its one production caller holds one), the view carries the lingering effects that hold, and `determinize` takes a shown number apart again — printed, lingering, table — where it had been folding the whole of it into printed strength, which would now count Echelon twice and had made every pump in flight permanent inside a rollout; a debug build asserts each sample agrees with its view. Random identical; the heuristic Runner buys fewer pumps and breaks more (441 → 414 pumps, 952 → 965 subroutines over the same encounters), outcomes unmoved (Runner 0.781 → 0.786, far inside the band). **Then ICE strength** (`feat/ice-strength-is-derived`): it was a number baked onto the run's copy of the ice from a `StrengthModifier` when the run began, on the stated ground that nothing moves it mid-run — and Syailendra places a counter on an Ice Wall mid-run, and Scatter Field's own subroutine can install the ice that ends its "only piece of ice". `continuous::ice_strength` is the one question the contest, `IceEncountered`, the view and the bots put; `StrengthModifier` and `RunIce::current_strength` are gone for two reads (`EffectRequirement::ProtectingRemote`, `Amount::IceProtectingThisServer`) and no `Effect`. In shadow over both 256-seed sweeps: two reads in one game disagreed (a Scatter Field shown to the Corp at 4 that was 0), no break contest did; with the old path deleted: identical, four reports of four. **Then the prohibitions, which closed the item** (`feat/prohibitions-are-continuous-effects`): Luminal Transubstantiation's score lock, Ansel 1.0's steal-or-trash bar and Tread Lightly's +3 to rez were a flag each with a reset each, two of them on fields no view carried — so no bot sample was ever bound by the lock or priced the +3. They are `GameState::lingering` entries about a player or each piece of ice (`lingering::On`), made by one `Effect::Prohibit { what, until }` where there were two single-use effects (26 of 72 variants single-use now), and `continuous::cannot` is the one predicate the guard and the action list ask. The field had hidden a rules bug: the rez handler added Tread Lightly's +3 to anything rezzed in the attacked server, so an asset or upgrade in its root cost 3 more than it prints where the card says "each piece of ice" — 32 such pricings in the 1,536-game shadow run, 11 more inside samples that had lost the modifier, and 5,422 reads of a score lock still standing on the Runner's turn, where it cannot matter. With the old path deleted: identical but for the renamed effect key, four reports of four. **Backlog item 3, "the first time each turn" as a query, taken up the same day in four stages, and its first is built** (`feat/a-turn-is-counted-where-it-is-heard`): `rules::turn_log` counts a turn's moments where they are heard — one `record` at the top of `dispatch_event`, keyed by the `Trigger` and a class of what the moment was about that holds only what both players saw, one `rotate` at every turn start — and five per-turn fields, their eight write and reset sites and three requirements went into it (`Amount::TimesThisTurn` / `TimesLastTurn` under the existing `AmountAtLeast`). In shadow over both 256-seed sweeps nothing disagreed but the flag everyone knew about: "a successful run this turn" was reset only when the Runner's turn began, so it read true through the Corp's next turn in 173,786 view builds, where the evaluator's run term read it; with the old path deleted: identical, four reports of four. `dispatcher::audit` now also fails an action that dispatches an event more often than its record holds it. **Then the view** (`feat/the-view-carries-the-turn`): `ClientView` carries both logs whole — they need no mask, having never counted what one player did not see — and each side's spent once-per-turn abilities, and `determinize` copies them where it had rebuilt a turn from two facts. `OncePerTurn` lost its free-form tag for the card and which copy of it, which is what makes the set maskable (a use by a facedown Corp install is left out of a Runner's view). Of 197,130 samples over both 256-seed sweeps, 32,575 had forgotten a successful run last turn (Public Trail and Measured Response were unplayable inside a sample), 5,521 the agenda points scored this turn (Neurospike dealt 0) and 29,034 a spent once-per-turn ability. Random identical, both shapes; heuristic moves as it should and inside the band (Runner 0.786 → 0.776; Neurospike played 0 → 1). **Then the word itself** (`feat/the-first-time-is-part-of-the-trigger`): "the first time each turn" is `first_each_turn` beside a trigger and its `when`, counting exactly what the entry listens for, and judged in the listener scan against the log as the event was recorded (`turn_log::AsOf`, which `plan_for` cannot be called without) — never before its own occurrence is counted, never after a nested event has counted a second, with the verdict carried on the queued trigger. A price asks the other question, none yet, on another type, so no card file writes a 0 or a 1. Eight cards whose source is active all turn are respelled and `FirstInstallThisTurn`, `FirstSuccessfulHqRunThisTurn` and their fields are gone. In shadow over both 256-seed sweeps the eight never disagreed; a ninth, "Knickknack" O'Brian, fired on a later run 155 times and went to the next stage, where a card that arrives late is the point. Identical, four reports of four. **Then the correction, which closed the item** (`feat/a-card-that-arrives-late-missed-the-first-time`): the eight cards that can become active mid-turn say `first_each_turn`, and a copy that arrives after the turn's first occurrence has missed it — over both 256-seed sweeps a DZMZ Optimizer had discounted a later program in 13 game-turns, Verbal Plasticity drawn on a later draw in 12, "Knickknack" O'Brian offered on a later run in 6 and Docklands Pass granted on a later breach in 2. `continuous::pay_install_cost_of`, the second scan that spent each discount at the real install, is gone: a price shown and a price paid are one question. Aggressive Trendsetting's "an **installed** Corp card" moved from an intervening if into the condition (`EventFilter::InstalledCard`, read off the trash event's new `install`), where a trash out of HQ is not the turn's first; Ryō "Phoenix" Ōno is the one named deferral. Heuristic identical, both shapes; random differs as it must (one game of 192, `knickknack_obrian/OnRunStart` 59 → 51). Across the item: `EffectRequirement` 41 → 36, `Amount` 14 → 16, `EventFilter` 2 → 3, `Effect` unmoved; seven state fields, seven reset sites and twenty-three tag strings gone. **Backlog item 4, generic prevention, taken up the same day in three stages, and its first is built** (`feat/what-is-prevented-is-a-word`): no card in the pool had ever opened the prevention window — `PreventDamage` and `PreventTrash` were both unused, and Shred, the one pool card that prints "prevent", went round it — so neither sweep had, and reading it found five things wrong that only an agent would have. `rules::prevention` is the one door damage, a tag and the trash of an installed card go through; what a card prevents is a word (`dsl::Preventable`, the payload of one `Effect::Prevent`) where it was an effect, a parked-state variant, an error and a pair of events per kind (`Effect` 72 → 71, `Trigger` 29 → 28). The window nests instead of taking the one window slot and never giving it back; only an interrupt or a pass is legal while the players are asked; nobody is asked about what nobody can prevent (matching word, requirement, cost); a selection-trash — every way the pool trashes a Runner program — goes through the door; and "the subroutines are not finished" is carried by whatever is parked through one function, where five hand-written sites had dropped it for a tag or a trace parked out of a subroutine's choice. Identical, four reports of four — as it must be while no pool card can open the window, which is the finding; **Then Shred** (`feat/shred-is-a-lingering-prevention`): its "the first time the Corp would end that run, prevent the run from ending unless…" was a field on the run — the third flag-with-a-reset after the score lock and Tread Lightly's +3, and like them in no view, so `determinize` wrote `None` and every sample of a Shred run ended it at the first "End the run". It is a `GameState::lingering` entry (`Lingering::PreventRunEnding`) that `prevention::run_ending` asks first and takes when asked; a prevention that stands is not an interrupt and has no window. Of 198,562 samples over both 256-seed sweeps 64 had Shred armed, every one a heuristic Corp deciding mid-run; identical, four reports of four. **Then the cards, which closed the item** (`feat/the-core-set-prevents`): Decoy, Net Shield and Sacrificial Construct are the first cards in the pool to open the prevention window, and two `DeckCategory::Sweep` decks — Eternal-legal, on identities no sample deck uses, rotated by both sweeps and yielded by `matchups()` never — are how an agent reaches it. Net Shield is a *triggered* interrupt, and writing it found that a trigger on damage about to resolve could never have fired in play (the dispatch queued its plan behind the very thing it was about); the cards that hear an announcement now resolve before the players are asked, damage is always announced so "the first time each turn" counts what came before the card did, and "net damage" is a third thing a moment can be about (`EventFilter::Damage`). A trigger's effect list and an access interaction's resolve through one `evaluate_sequence` with `Effect::Sequence`, or Snare!'s damage would have landed underneath the window its tag opened. The three cards would have reindexed the observation vocabulary under every Elevation card; they append instead (slots 178..=180, pinned). Over 192 random games of the two decks: 719 announcements, 66 preventions, every game ended; the heuristic Runner installs none of the three, which is owed to Phase 5 as a bot blindness. One correction rode along, measured apart: 0 damage was recorded and dispatched as `DamageTaken { amount: 0 }` (28 of 371 in one random pass of the pool), and is not an occurrence now. Against `main` the random reports differ in those two event keys and nothing else; the heuristic ones move broadly (Runner +0.042, inside the band), and with the three card files moved out of the build they too differ in the two keys alone — the movement is the registry growing under `determinize`'s registry-wide pool, not the engine. Across the item: `Effect` 72 → 71 with two of its five unused variants now one that three cards use, `Trigger` 29 → 28, `EventFilter` 3 → 4, one run field and four events gone. **Backlog item 5, where a payment comes from, taken up (21 September 2026) in four stages, and its first is built** (`feat/a-payment-has-one-door`): credits that are not the credit pool lived in six places, each spent by a different piece of code with its own affordability sum beside it, and every such sum forgot a pool the payment then took. `rules::payment` is the one scan — the site says what a payment is *for*, a card says what its hosted credits pay for (`dsl::PaysFor`, where it was `hosted_credits_usable_for`), and whether a cost can be paid and paying it read the same list. Five sums disagreed, two of them failing on `main` at play level: a trash cost could not be paid with bad publicity or Overclock's credits, so the trash was never offered, and a Runner traced mid-run could not bid them. Live and rare: on a shadow build, 2 game-turns in 192 random games and none in 192 heuristic ones; against `main` the heuristic reports are identical and the random ones differ by exactly one game of 192 (seed 68), which is the whole aggregate movement. Two of the six pools are reached by no agent — no deck plays Hostile Takeover or NBN: Making News — and are owed to the sweep decks. **Then recurring credits** (`feat/recurring-credits-are-declared`): "N[recurring-credit]" is a declaration on any card, hosted as its counters, placed when the card becomes active and refilled as a step of the turn — Comprehensive Rules 1.10.5a, "before abilities meet their trigger conditions for your turn beginning". Azimat and Mahkota Langit Grid did it as an `OnTurnStart` trigger, resolved among the abilities the rule puts it before, and **a player with one other turn-start ability was asked which to resolve first** — confirmed on `main`, where a Runner with Azimat and Open Market is parked on a `ChooseTriggerOrder` of exactly those two. NBN: Making News did it a third way, in two `CorpState` fields; it hosts on the identity now, and had no test and no deck, so a green suite had said nothing about it. `Effect::RefillCountersTo`, both fields and `RecurringCreditsSpent` are gone (`Effect` 71 → 70; 26 of 70 single-use, 3 unused); the view keeps its two recurring-credit values, derived, so no observation slot moves. All four reports differ, as a removed decision must make them: order prompts fall by a quarter (random `ChooseTriggerToResolve` 581 → 437, heuristic 261 → 188), `CountersAdded` is flat so the refill still reaches the cards, outcomes move under one standard deviation, and the one red flag written down beforehand that tripped — a trigger key gone from the random report — was a count of one that the heuristic reports fire 11 times before and after. The heuristic Runner never installs Azimat, so its half is measured by the random seats alone. **Then the choice** (`feat/the-payer-chooses-the-pool`): a pool has a class — what its credits pay for and how long they last — and the payer is asked only when the answer changes what they are left with: a pool never worth more than another is spent first unasked, and what remains is two pools neither the other's lesser and a payment too small for both (Azimat's, trash costs only and back next turn, against a run's, anything and gone with it — the fixed order drained Azimat first). Answered by the existing `ResolvePendingChoice`, so no `ActionSpace` growth; splitting one payment credit by credit waits on item 6. **Parked by replay:** the payment unwinds the action, `apply_action` returns the untouched state with the question on it, and the answer applies the action again — some thirty paying handlers never learned to stop halfway. Reading found two things no test would have: the planner could loop forever on an answer that was not on offer, and **the log was a second channel** — a parked action is submitted, not applied, yet the log mask shows a played event whole, so the other seat's log would have named a card their view was withholding (`mask_logged_action_for_player`; both masks tested on the wire and shown able to fail). The prediction was written before the run and held: heuristic reports identical, random differing by **one question in 192 games and nothing else**. So the mechanism is carried by scripted tests, not the sweeps, and the gate that holds the prompt to having been used belongs to the next stage's decks. **Its cost was measured because the PR had to admit it had not been:** parking needs a copy of the action made before it is applied, and copying on every application cost 2.4–3.8% of a byte-identical heuristic pass — all of it the copy, a build without it running at 0.9998 of `main` — because the legal-action probe applies every candidate. The copy is made only where a question is possible (`payment::could_ask`, an allocation-free necessary condition whose reports hash exactly as the ungated build's): +1.0%, the scan itself. **Then the cards, which closed the item** (`feat/the-core-set-pays-from-pools`): Cyberfeeder, The Toolbox and Crash Space are card files — two `PaysFor` words, `Purpose::Ability` stated at all three sites that pay for an ability or ask whether anybody could, and link asked of `continuous::link` rather than kept on `RunnerState` — with two sweep decks (Pay As You Go on Noise, Hostile Bid on Weyland: Building a Better World) that make pools compete and reach bad publicity for the first time. **The 256-seed view sweep on those decks found a deadlock in merged stage 3 that the default bar and CI had passed:** a handler pays and then does the thing, so an action that was going to fail parked on its payment's question, was offered as legal, and every answer replayed into the error (6 of 192 random games of the two decks stalled, the headless CLI exiting 0 on each). An action parks only if some sequence of answers completes it. The re-measurement found a seventh stall that was no payment at all — Noise's "trash the top card of R&D" was an error against an empty R&D, on an identity no deck had ever played — and after it 192 random games end, 0 stalled. The break helper, which plays routes on the engine, had lost every route wherever pools compete and now answers and waits. Against `main`: random identical, both shapes; heuristic moves broadly and inside the band (Runner −0.021), and with the cards and decks moved out of the build all four reports are identical, so the movement is the registry growing. The payment question is asked 41 times in 192 games of the two decks and about once in 768 sweep games — reach, not coverage, and gated as rare. Owed: the heuristic Runner installs no pool card (Phase 5), no card in the pool starts a trace, and splitting a payment credit by credit is item 6, a numeric decision, which is next. **Backlog item 6, a numeric decision, taken up the same day in two stages, and its first is built** (`feat/a-number-is-a-decision`): the pool already printed chosen numbers, and each card that did had taken the most its text allows — Bigger Picture's "remove any number of tags" removed them all, which gave away the decision the card is. `Effect::ChooseNumber` parks a range, every number in it a legal action the way a trace offers every bid, answered by its own `PlayerAction::ChooseNumber` on a segment **appended** to `ActionSpace` (1646 → 1677, no index moved; `OBS_SIZE` unmoved on purpose). The number is written into the effect that waits (`Amount::ChosenNumber`, a placeholder `Effect::with_chosen_number` writes over), because on a `ResolutionContext` it would not survive a park — and Bigger Picture's credit loss comes after a tag removal Synapse Global answers with a prompt of its own, a pair one sample deck plays. The first card test found that Account Siphon's optional replacement resolved with no acting card, so a continuation could not be pinned and the `EndTheRun` behind its new prompt was dropped; the replacement remembers its card now. Against `main`: heuristic identical, both shapes; random differs in exactly 3 games of 192, each one Bigger Picture asked for a number, end-reason totals unmoved — as predicted, including that 3 in 192 is reach and not coverage, so the action is gated as rare and held by 101 numbers chosen over 576 random games of the three deck pairs that hold both cards, none stalled. Next: the payment split, credit by credit. | [rules-audit.md](docs/roadmap/rules-audit.md) |
| **Phase 1.75** — Learn to Play | Done. Seven lessons a side, the starter game at 6 points. | [phase-1-75-learn-to-play.md](docs/roadmap/phase-1-75-learn-to-play.md) |
| **Phase 2** — Bots, replay, gym, training | §1–§4 done. **§5 open: six volume runs and no candidate ever promoted.** The observation rework bought nothing (item 20); item 21 blamed the leaf regime the network's evaluator never got. **Item 22's screen retired both that and staleness**: a HEAD-trained checkpoint scores the same 0.141 `value-only` as the stale one despite beating its chair null, and the anchor is worth +0.008 there and −0.101 in `both`. **The live finding is that the priors alone score 0.617** — over the promotion threshold, both chairs above baseline, a first in five runs — and the value head drags them to 0.359. That engineering landed (items 23, 24) and **the fifth run answered its question: no** (item 26). Three candidates, three rejections, pooled 0.4878 over 1,152 games. Its self-play runs ~65% Corp against the engine's 54.8% over four disjoint seed ranges, which is the same distortion on the input side. **Items 25 and 27 priced the loop and then cut it**: a network-in-the-loop iteration cost 4.2 h against a uniform one's 0.26 h, because inference throughput does not scale (5,029 rows/s at one thread, 5,391 at seventeen) and tree work evicts the 4.6 MB of weights between calls. **Batching rows across concurrent games is 2.6× with a byte-identical corpus** and cut resident memory 411 → 175 MB by sharing four sessions instead of ~36. **Items 28 and 29 then built a per-chair promotion floor beside the blend and a `(chair, outcome)` sample weight** worth +0.073 on the Runner chair at strength 0.5, over-correcting at 1.0. **The sixth run then took that correction into the loop and it did not survive** (item 30): six iterations, six rejections, pooled **0.457 blended over 576 games**. Its self-play is *more* Corp-skewed than the uncorrected loop's — **70.5% over 7,200 games against 64.9%** and the engine's 54.8% — and replacing the whole training window with corrected data moved nothing, every column's observed sd falling below its own sampling sd. That run also corrects how the fifth was described: with no promotion the generator never advances, so both were one fixed generator with a growing corpus, and the sixth had to bootstrap its incumbent to be a loop at all. **Item 31 then priced the last untouched suspect and it is not the answer either**: root Dirichlet noise landed default-off and byte-identical, and over 5,760 self-play games it moves the generator's Corp share **64.6% → 61.8% (z = −2.08)** — a quarter of the 10.1-point gap to the engine's 54.8%, with `ε` saturating by 0.25 and a per-node `α = 10/n` landing on the fixed `α = 0.3`'s number (62.4% against 62.6% on the same 1,536 seeds). The screen's own numbers relocate the distortion: **the uniform search's corpus is balanced at 54.8% and the skew appears only once the priors distilled from it are seated**. **Item 32 then ran the chair diagnostic that relocation asked for, and answered it no** — over 82,459 held-out uniform-search decisions the head is equally faithful on both seats (visit-share regret **0.0651 Corp against 0.0657 Runner**; the 7.5-point top-1 gap is difficulty, halved by standardization and gone when priced in the search's currency), calibration is equal (ECE 0.0032 / 0.0035) and every offline chair figure is flat across item 29's four `--chair-balance` legs while their arena Runner chair moves 0.250 → 0.323. **The seat trade itself was an artifact**: two null legs (candidate = incumbent, seated as each run seated its own) score exactly 0.500 with zero draws at **Corp 0.6823 / Runner 0.3177** and **0.7135 / 0.2865**, so a chair's parity is the pool's Corp win rate, not 0.5. Items 26, 28, 29, 30 and 31 are corrected in place. **Item 33 then re-centred the gate on that null and it can promote again**: `--arena-null` seats the incumbent in both chairs, the loop measures one per incumbent per arena shape (~20 min at 384 games, ~5 at a 96-game screen, cached), and `--promote-chair-margin 0.07` / `--arena-screen-chair-margin 0.20` replace the absolute 0.45 / 0.30 — sized on the sd of the *difference*, since the null is measured too. Replaying both runs' recorded verdicts, **seven of nine reasons change and no verdict does**: five of the sixth run's six rejections stop being chair-floor verdicts (three go to a full arena, two to the blend), while its one full arena still collapses on the Runner chair at −1.7σ. The Corp skew survives as a fact about the *pool under sharper play* — it moves both players together. **Item 34 then measured that directly on `bench` and it is search budget, not the network**: with the uniform evaluator and one bot in both chairs, the pool runs **0.549 → 0.586 → 0.703** Corp at 32 → 128 → 512 simulations (+0.117, 5.5σ), saturating from 512 on and landing on the same 0.68/0.71 the head's greedy legs did, with no network in the process. It is the **Corp chair** converting budget (0.458 → 0.581 → 0.714 against a fixed heuristic) while the Runner chair gains nothing past 128 (0.409 → 0.513 → 0.461), and it is the whole pool — 15 of 16 Corp decks and 12 of 12 Runner decks. Item 31's ten points are explained and items 31/32's open question is closed. What it hands over is a bound: `mcts@128` is the pool's best Runner (**+0.077 over `puct@128`, 2.95σ**) and its worst Corp, so 0.70 is an upper bound on the pool's Corp bias measured with a beatable Runner. **Item 35 then answered what MCTS's Runner chair was doing, and it is not the rollout**: the whole lead is `MctsAgent`'s root-parallel **tree count** — a number read off `rayon::current_num_threads()`, so `mcts@128` was 4 × 32 and a different bot on a smaller box. At one tree the two searches land within 0.003 (0.5104 against 0.5130, z = −0.09) while still differing in leaf evaluation, exploration rule and backup. It is the **samples**, not the depth they cost: at an identical 32 iterations, one sample scores 0.497 and four score 0.635 (z = +4.90), while at one sample 32 → 128 is worth nothing (z = −0.48). **The two chairs are bound by different resources** — over 16 → 128 iterations at one sample, depth is worth +0.208 to the Corp chair and still climbing, and +0.076 to the Runner chair, saturated by 64 — which is item 34's budget curve from the inside. **The gain is the hidden information itself**: four trees sharing one determinization score 0.5417, −0.094 against four independent samples and only +0.031 (n.s.) against one. **PUCT's same knob is negative on both chairs** (Runner 0.513 → 0.411, z = −3.22; Corp 0.581 → 0.492, z = −2.79), landing on `puct@32`'s number — it buys nothing and pays the depth loss — which confirms Phase 3 §1's null rather than overturning it. **Item 36 then answered item 35's open question without playing a game, and answered it at the limit** (10 September 2026): `diag leaf-sensitivity` sweeps the plies between a determinization and `evaluate_state_with` — depth 0 is `puct`'s leaf, 16 is `mcts`'s, and both bottom out in that one function so they share a scale with no normalization. **At depth 0 the leaf is bit-identical across all 16 samples in every one of 384 Corp positions**, the root argmax unanimous 1.000 Corp / 0.992 Runner (1.000 on `puct`'s own positions): PUCT marginalizes over nothing. It is structural — every term reads public counts, board state or the evaluating side's own zones, the sole exception being `pending_decision_upside` over a parked `ChooseCards`, and `run_is_breakable`'s doc comment says outright that "unrezzed ICE is treated as passable". At 16 plies, paired against a shared-sample control, the sample costs argmax agreement **+0.070 (z = +7.74)** on the Runner chair against **+0.007 (z = +1.57, n.s.)** on the Corp's, reproduced on a second seed (+0.071 / +0.007) and on `puct`-drawn positions (+0.077 / +0.019) — item 35's chair asymmetry with no games in it, and the reason four samples cost the Corp chair depth for nothing. **Item 37 then found, while starting item 1, that the determinization had no ICE to price** (11 September 2026): `determinize_run` drew a masked ICE's identity correctly and then filled the rest of the `RunIce` with **`Barrier` and no subroutines** whatever it drew, and `reconcile_ice` never repaired it because it keeps an entry whose `card_id` still matches — which it does by construction. **Every search rezzed a toothless Barrier, on every sample, for as long as `determinize` has existed.** Now built the way `run::engine::build_run_ice` builds it. Strength effect nil (128 games a pairing at 128 simulations, paired: `heuristic` vs `puct@128` +0.016, z = +0.58; `heuristic` vs `mcts@128` −0.016, z = −1.41), and the three **byte-identical** cells are the evidence it lands where theory says — a Corp is never masked from its own ICE, and the `heuristic` is blind to an unrezzed one on either chair. It also revises item 36: that entry's 16-ply sensitivity came from sampled HQ and R&D, not from what was behind the ICE. **Item 38 then built the term item 1 asked for, and closed item 1 negatively** (11 September 2026): `unbreakable_unrezzed_ice` does exactly what item 36 said no term did — with it on, the Runner's depth-0 leaf stops being blind (argmax agreement across 16 samples 0.998 → 0.968, paired cost +0.002 n.s. → **+0.032, z = +4.49**) — and it buys nothing. Against a fixed heuristic Corp (fixed for real: the term is Runner-arm only) over 192 games a pairing at 128 simulations, PUCT's Runner chair gains **+0.016 and +0.010** on two seeds at weight 0.6, both below the 0.026–0.047 band and n.s., and **loses 0.036 and 0.057** at 1.5; a one-ply Runner is hurt outright, `heuristic` vs `heuristic` moving +0.05 to +0.06 to the Corp in all four legs. Ships at **weight 0.0** — apparatus, not behaviour, and byte-identical to the baseline. The reason is the keeper: the term treats an unrezzed ICE as certain to be rezzed, and a real Corp declines. **The information is there and the leaf can see it; what is missing is a probability, not a reading.** **§5 item 39 then measured that probability and corrected the sentence before it** (12 September 2026): `diag rez-rate` records one observation per approach to an unrezzed ICE, over the term's own predicate, with every leg checked complete against `GameEvent::IceApproached` (six legs, 1075/1075 through 1397/1397). The `heuristic` Corp — the one in item 38's binding cell — rezzes what the term counts **967 of 1,032 times (0.937)**, its whole 6% of declines sitting at printed cost 8+ (0 of 42), so there is no discount to apply and item 38's explanation is wrong for the Corp it measured. **The failure is the other half of the claim**: of the counted ICE the Corp rezzed, the run stopped there only **0.47–0.55**, splitting into **0.904 with an ETR subroutine and 0.077 without** (686/759 against 49/633) over an even 54.5/45.5 division of what the term counts. `cheapest_break_cost` returning `None` means no rig card can break it, which the term reads as "this stops you"; an ICE that only tags or does damage stops nothing. **So it is a reading after all — `SubroutineDef::effect`, in the definition the term already looks up — and the fix is a filter, not a discount.** A *searching* Corp does decline as item 38 supposed (`puct@128` rezzes 0.487/0.574, and 0.08 at cost 5–7), so a rez rate now has to say which Corp it was taken over. **§5 item 40 then built that filter and closed the line** (12 September 2026): `Effect::can_end_the_run` narrows the term to ICE that actually ends the run, and on identical games it drops the count 282/308 → **152/149** while the stop rate rises **0.53 → 0.895/0.926** and the rez rate to 1.000 — the predicate is right now. It also repairs item 38's clearest negative: a one-ply Runner was hurt +0.05 to +0.06 in all four legs, and paired unfiltered-against-filtered on the same games that harm is **−0.052 to −0.083 (z to −2.74)**, gone. **And the repaired term still buys nothing** — against the weight-0.0 baseline the binding cell reads +0.026/−0.026 at weight 0.6 and −0.016/−0.016 at 1.5, nothing clearing the 0.026–0.047 band on both seeds at one weight, so it ships at **0.0** again, byte-identical to the baseline. Two negatives in a row on a leaf that now reads the hidden state correctly is the answer to item 34's binding constraint: **PUCT's Runner chair is not leaf-bound**, and item 35's +0.125 for `mcts` was the HQ and R&D contents its samples differ in, exactly as item 37 said. `scripts/paired_bench.py` (McNemar over discordant games, checked against item 38's recorded numbers) is the apparatus and stays. **§5 item 41 then made `mcts`'s tree count a decision rather than a core count** (12 September 2026): `with_iterations` took `rayon::current_num_threads().clamp(1, 4)`, which reads like a parallelism setting and is not one — the budget is split `iterations / trees`, so it trades hidden-information samples against depth at fixed cost, the trade item 35 measured as +0.125 to the Runner chair and −0.022 to the Corp's. It is now `DEFAULT_TREES = 4`. The defect reached games through `--threads`: **52 of 192 differ between `--threads 2` and `--threads 18`** on the old binary and `mcts`'s Runner chair loses **0.230**, while after the fix the two thread counts are identical game for game and `--threads 18` is unchanged from before — so every number recorded on this box stands. It was also rating a machine-dependent bot on the server's `HumanVsBot` ladder, and giving each CI platform a different `mcts` in `puct_seat.rs`. **§5 item 42 then made `mcts`'s rollouts 39% cheaper without moving its policy** (13 September 2026): a playout ply spent 38.1 `apply_action` calls proving candidates it would not play, and `rules::apply_sampled_legal_action` validates only what the weighted draw lands on (18.0), drawing from exactly the distribution `legal_actions` gave. Paired over 384 games a pairing the same bot at equal simulations moves nothing significant (Runner chair −0.036, z −1.46; Corp +0.016, z +0.51), and the random-vs-random report is byte-identical. It is the prerequisite for measuring the Runner chair past four samples, where item 35's curve was still rising. **§5 item 43 then measured it, and the Runner chair plateaus** (13 September 2026): against a fixed one-ply Corp over 384 identical games a cell, 4 → 8 → 16 → 32 trees score **0.604 / 0.609 / 0.622 / 0.604** and 4 × 128 scores 0.615 — samples saturate at four and budget buys nothing. Playout depth peaks at the default 16 (4 × 32: 0.440 / 0.604 / 0.547 / 0.487 at 8 / 16 / 32 / 48 plies) and is a *complement* of samples, as item 36 predicted (1 → 4 trees is +0.073 at 16 plies, +0.141 at 32), but the best cell, 16 × 32 at 32 plies, is 0.635 — +0.031 over `elite`, z +1.12, inside the 0.037 the `elite` itself moved across item 42's re-roll. It changes how the Runner loses (flatlines 55 → 26, Corp agenda wins 97 → 114), not how often. The ladder is unchanged; `bench --pairing`, `--mcts-depth` and `paired_bench.py --by-kind` stay. | [phase-2-bots-and-training.md](docs/roadmap/phase-2-bots-and-training.md) |
| **Phase 3** — Personalities and rating | Done, and **reopened for the player-facing half** (13 September 2026). Five archetypes measured across seeds; Glicko-2 ladder. **A deck now carries its style and a rung keeps it** (`DeckFile::style`, all 28 sample decks tagged from their lists, one resolver — flag, else deck, else balanced — in the CLI and the daemon; `--corp-level 4` had silently played `Balanced`). Measured as a signature over 96 heuristic games: `CardAdvanced` 868 → 910, runs 3,106 → 3,362, Runner draw clicks 246 → 110, outcome inside the band. **Two more Runner styles and no fourth Corp style** (same day): five candidates measured by signature over three seeds, `Builder` (hardware and resources installed at 3× balanced) and `Wary` (the one term that reads an unrezzed card, switched on) shipped; `Tagger` was byte-identical, `Warden` flat on two cuts (the rez is credit-bound, not value-bound), `Banker` cost −0.066 of win share, `Opportunist` either never ran again or was balanced with noise. The one-ply Corp's three card-reading levers are already Rush, Glacier and Trap; a fourth Corp style needs a new lever or a search. **Local play is rated** (same day): every TUI game against a bot lands on the `HumanVsBot` track in `<data dir>/netrunner/ratings.json` with a game log beside the book, a quit from turn 3 is a forfeit, the game-over modal shows the new rating and `Next: try operator (3)`, and `netrunner_cli ratings` prints the ladder. **Corrected 20 September 2026 (`fix/local-play-is-casual`): local play is casual, not rated.** A rating kept in a file the rated player owns certified nothing, and guarding it taxed practice — an undo past what a move had taught ended the game's rating. The client's Glicko-2 book, `--unrated`, the rated toggle and the undo's cost are gone; `Track::HumanVsBot` left the rating crate, so the daemon rates only two people; the game log stays as `record.json` (read once from the old `ratings.json`) because the rung suggestion only ever read the log, and `netrunner_cli record` prints it. The suggestion is the last five results against the rung played, not the Glicko-2 expected score — which between two participants who only play each other flips on the last game (WWWL → 0.568, WWLL → 0.261). Still open here: the start screen — the "next" list below. The Corp run term landed; the damage term is measured impossible at one ply. **PUCT's Runner chair was choosing by noise** — root-relative leaf values took it from 0.219 to 0.411 against the heuristic Corp and the Corp chair 0.417 → 0.510, then the run term kept through the breach took the Runner to **0.516** (§1). **The heuristic Runner on that seating is 0.417, not the 0.583 quoted** — re-measured on the pinned binary; the PUCT Runner is 0.10 ahead of one ply. Determinization now draws from the identity's published decklist (exact partition of the real deck); it bought fidelity, not strength (0.516 → 0.500), and the breach chance node still loses over the honest pool. Carrying identities into the sample was recorded negative for the search; over three seeds that **does not reproduce** — it changes sign and sits inside the band, and the claim is withdrawn (§1, last bullet). The breach chance node does reproduce (−0.038, sd 0.008) and its budget explanation is measured away; the search is not budget-bound at all (128 → 512 simulations, 0.483 → 0.481). Seed-spread band on that seating: **0.026–0.047 over 192 games**. |
| **Phase 4** — Network | §1–§3 done (masked log, reconnect, registry/lobby/clock/spectators, the published pool). §4 deltas deferred until profiled. **§5 open, design only (20 September 2026): a rating is a server's to keep** — identity is a client-held Ed25519 key proved by a challenge that names the server, each player signs a seat commitment and the server signs a receipt over the stored match record, and the results log is the truth the rating book is rebuilt from ([docs/identity-and-rating.md](docs/identity-and-rating.md)); five stages, none built. | [phase-4-network.md](docs/roadmap/phase-4-network.md) |
| **Phase 5** — Difficulty ladder | **§1-§2 done (13 September 2026); the daemon seats a rung (`--bot-level`, the same bot as the TUI's `--corp-level`; unrated since 20 September 2026, like every game against a bot); a start screen picks chair, rung, style and decks when no side flag is given, folded into the flag path.** Five named rungs per chair (`--corp-level`/`--runner-level`, `bench --bots level:elite`), built by *handicapping* a strong bot rather than dialling search down — because search is not a difficulty dial on the Corp chair: `puct` does not overtake one ply there until past 128 simulations, and the first cut calibrated to a flat 0.562 / 0.500 / 0.458 across its middle three rungs. `HandicapAgent` makes the order structural (`epsilon` 1.0 is random, 0.0 is the bot itself), pinned by a test that every rung is the one below it with less handicap. Calibrated at 48 games a pairing: the bottom two steps are **+0.250 / +0.208** (Corp) and **+0.146 / +0.208** (Runner), all resolved; the top steps are 0.04-0.17 against a sampling sd of 0.10 and only `veteran → elite` as Corp is resolved. **The Runner chair's whole span from one ply to the best bot is 0.104 against the Corp's 0.229**, so its top two rungs are ~0.06 apart and five distinguishable Runner rungs do not exist until there is a better Runner — Phase 2 §5's Runner-chair work, now with a user-facing consequence. **A wider `elite` Runner is not that better Runner** (Phase 2 §5 item 43): 4 → 32 trees, 128 → 1,024 simulations and 16 → 32-ply playouts all land at 0.60–0.64. **The better Runner was the one-ply bot with a leaf that reads the board** (15 September 2026, Phase 2 §5a reopened): 0.417 → **0.865** against the fixed heuristic Corp, ahead of `puct@128` (0.760) and `mcts@128` (0.677) on the same games, so the Runner ladder is now one ply at five handicaps — the search rungs would have lost to rung 3. **The owed calibration then ran at 768 games a cell (the full 5 × 5 square at `--games 384` on each of two seeds, 19,200 games a spec) and both chairs climb at every step, which is a first.** It found the first cut's Runner rungs crammed at the top — steps of +0.324 / +0.185 / +0.155 / **+0.044** against an even 0.177, the last `flat` on one seed of two — and `epsilon` near-linear in win rate there (w ≈ 0.833 − 0.70ε to 0.034), so interpolating for even steps gave ε = 0.73 / 0.46 / 0.23 and the spec took the round numbers **1.00 / 0.75 / 0.50 / 0.25 / 0.00**: re-measured, **+0.133 / +0.195 / +0.211 / +0.169**, every step a rise at z ≥ 6.7 and on each seed alone, with exactly the 10 cells whose Runner is `novice` or `elite` byte-identical between the runs. The Corp chair was already even (+0.066 / +0.089 / +0.055 / +0.045 against 0.064) and is untouched. **What the run found unasked is that the Corp chair now has no top**: `puct@512` scores **0.266** against the un-handicapped one-ply Runner where §1 measured 0.714, because the Runner's new evaluator moved the whole pool (the 25 cells run 0.385 → 0.471 Corp across the two specs, against the engine's 0.548), so a Runner-seated player has no hard opponent at any rung. That replaces the calibration as the phase's open item. **§3 is the first cut at it (16 September 2026)**: the Corp arm of the evaluator read nothing of the Runner's board, and priced ICE by `REZZED_ICE_WEIGHT − 0.4 × cost` — a formula that *falls with cost*, so the Corp rezzed its weakest ICE (Tithe, 1[c], no run-ender: 0.50) and left its best face-down (Pharos, 7[c], two run-enders: **0.26**; Brân 1.0: 0.24), meeting 3,781 runs with 2,050 pieces of ICE and losing 86% of them. Two mirrors of terms the Runner already had — a run-ending subroutine on a rezzed piece at 1.0, an uncovered subtype at 1.2 — put Pharos at **0.52**. The win-rate effect is small and is stated carefully because the first reading of it was wrong: `--corp-personality` unset is *the deck's own style*, not `balanced`, so comparing an unset baseline against a forced-balanced result overstated it as 0.161 → 0.204 at z = 2.18. Like for like over 768 games on four seeds it is **0.188 → 0.204** forced balanced (z = 0.84) and **0.161 → 0.194** on deck styles (z = 1.67) — neither significant alone, but a rise on all eight seed-pairings (p ≈ 0.004 on a sign test), and the rez mechanism is not statistical at all. Three things measured and rejected: a Corp `HELD_CARD_WEIGHT` is a switch rather than a dial and costs 0.182 → **0.083** (the Corp is credit-starved, not card-starved — it cannot pay to rez what it already installed), `glacier`'s bigger fort scores 0.174, and with the two terms in, `REZZED_ICE_WEIGHT` 1.4 → 100.0 is byte-identical because every affordable rez already happens. **The Corp personalities also need re-auditing against the new rez arithmetic, and `Glacier` has a named defect**: "build the fort first" rezzes the *cheapest* ICE of any Corp profile (mean cost 3.12, against `Rush`'s 3.24) because its flat `rezzed_ice_weight` 1.8 helps all ICE alike while its `own_credit_weight` 0.5 is a per-cost penalty biting hardest on the ICE it exists for — and balanced beats it 0.198 to 0.174. The one-knob repair is not the repair: dropping that weight to 0.4 scores 0.156 and to 0.3 scores 0.151, both worse. A profile retune is its own measured job. The next lever is measured and not yet taken, and is bigger than anything this entry bought: `trap`'s three card-reading ambush terms score **0.259 ± 0.032** against the same build's forced `balanced` 0.204 ± 0.029 over 768 games each (z = 2.6), with flatlines 32 → 79 — both sides name a personality, so the style confound above cannot touch it. **§5 opens §4(c) with the instrument it needs (16 September 2026)**: `netrunner_cli diag tempo` reports one record per turn per chair — that turn's clicks by what they bought, on a snapshot of the board they were spent on — because every figure in §3 and §4 is a win rate and a win rate cannot tell "plays in phases now" from "plays the same and wins slightly more". The baseline (384 games, both chairs `heuristic:balanced`, seed 2 reproducing every column within 0.02) finds **no phase in either chair**: the Corp installs on every turn and advances on every turn from the second — its *advancement peak is turn 2*, before any fort exists — and **the Runner's stance is inverted**, installing almost everything on turn 1, stopping by turn 3, never covering more than **1.43 of 3** ICE subtypes, while its runs peak at turn 2 (2.20) and fall to 1.00. It is most aggressive when its rig is emptiest, which is the reverse of `Builder`'s and `Aggressive`'s own doc comments and makes the Runner chair a *larger* §4(c) target than the Corp one. **It also corrects §3 in place**: "the Corp is credit-starved, it cannot pay to rez what it already installed" is true only early — credits at turn start run **5.0 → 3.2 → 27.0** while face-down installs climb monotonically to **5.61** and the rez rate *falls* 1.04 → 0.34 a turn, so late in a game this Corp is rich, holding five unrezzed cards, and rezzing less than ever. A "credits against the rez costs on the table" term would be inert exactly where §3 implied it would bite. **§6 then built §4(c)'s mechanism and measured it off** (16 September 2026, Runner chair first, because §5 found that chair's stance *inverted* rather than merely flat): four of the seven personalities describe themselves with a temporal word they cannot act on (`Glacier` "build the fort, **then** score behind it", `Rush` "score early, protect late", `Builder` "the rig first… **before** the runs start", `Cautious` "a full rig **before** a run") and each pair moves *the same fields in opposite directions* — so they are the two ends of one dial, and `stage_weights` interpolates `Weights` along it under a board-read scalar (`breaker_coverage`, overridden by the Corp's clock) rather than adding six new terms. That also explains a number §3 recorded without one: balanced beating both `Builder` and `Aggressive` is what a fixed midpoint of a moving dial looks like against its own ends. **It costs the Runner chair 0.159 and 0.180 on two seeds** (Corp 0.143 → 0.302 and 0.133 → 0.312 over 384 games a leg, z 5.90 and 6.49), monotone in the dial (−0.081 at half gain), far outside the 0.026–0.047 band in the wrong direction. **§5's instrument says why**: the Runner does change stance as designed — credit clicks 10.3 → 6.1 a game, draws 3.3 → 5.0, installs 1.9 → 2.6 — but its **rig coverage falls while its rig grows** (0.83 → 0.30 at turn 2; size 1.55 → 1.71 at turn 8), because `Builder`'s flat `board_presence_weight` 1.6 rewards *any* rig card and beats the `breaker_coverage_weight` 4.0 that is the profile's purpose. **That is `Glacier`'s named defect from §3 on the Runner's side of the table, and the dial makes it self-reinforcing**: the scalar is gated on coverage, so a Runner filling its rig with non-breakers never leaves the build stance. A static `Builder` plays badly; a staged one is trapped. So the suspect is the *endpoints*, not the scalar — a build endpoint has to reward coverage rather than presence — which is the endpoint repair §4(b) already owed for the Corp, now owed for both chairs with a measured reason. Ships at `STAGE_GAIN` **0.0** on Phase 2 §5 items 38/40's precedent, verified byte-identical to §5 game for game. **§7 repaired the endpoint and re-measured the dial** (16 September 2026): `builder` was not a weak build end but the *worst* Runner profile in the pool — the balanced Corp won **0.305** of 2,304 games against it, against 0.148 for balanced — and an ablation put all of it on one knob, the flat `board_presence_weight` 1.6, which made a 1[c] non-breaker (+1.2) outbid the click that saves for a breaker (+0.7), so the profile sat on 3.5 credits by turn 3 and installed one breaker subtype per 1.6 rig cards. At **0.4** (one credit) the rig is breakers (coverage 1.12 on 1.14 cards at turn 5) and the Corp's share falls to **0.121** (z 16.4, six seeds), more than halved against every Corp profile. The dial's cost is gone with it (gain 1.0: 0.309 → 0.149, z 11.8) — and the travel still buys nothing (+0.009 against static, z 0.83), because **no leg that moves beats standing on the repaired endpoint in either direction**: `builder` all game 0.113, `builder` → balanced 0.125, balanced → `builder` 0.137, `builder` → `aggressive` 0.149. §4(c)'s "build, *then* press" is not supported on the Runner chair; `STAGE_GAIN` stays 0.0 for that reason. Handed over: the same knob on balanced's constant (moves both chairs and the calibrated ladder), and `Glacier`'s flat `rezzed_ice_weight` — the analogue of this repair on the Corp chair, untried. **§8 measured the owed turn-counter fallback** (same travel on the Runner's own turn count, horizons 4/8/12/16, both destinations): it never beats the board scalar beyond noise (best −0.014, z 1.29), loses to it outright on a short clock (turn 4: +0.040, z 3.56), and never beats standing on `builder` all game (best +0.003) — so neither schedule §4(c) named earns a gain on the Runner chair. **§9 audited the Corp profiles the same way** (16 September 2026): `glacier` was burying its hand. Its `unrezzed_install_weight` 1.2 let a face-down install out of a thin HQ beat its own credit click, so it put 2.3 cards face down on turn 2 with 3.6 credits to rez them. Past that switch (0.8) it banks first and installs the same cards later, when it can pay, and protection wanted 3.0 rather than 1.0: **0.168 → 0.282** against the balanced Runner (z 10.9, six seeds), +0.10 to +0.12 against every Runner profile, ahead of `trap` by 0.070 (z 6.1). **Unlike §3's terms, it survives search**: `puct@512` as Corp 0.284 → 0.357 (one seed, z 2.5; 0.245 as balanced), so the Corp's top rungs have a lever that is not more search. §3's suspect, the flat `rezzed_ice_weight`, measured as helping, not costing. `rush` loses to balanced by 0.058 because of the two knobs that make it a rush, and is recorded, not repaired; `trap` is its ambush terms and is unchanged. Handed over: a Corp with both `trap`'s and `glacier`'s levers, and the fact that play seats bots in their deck's style (five decks are `glacier`) while the rungs are calibrated as balanced. **§10 measured that Corp ladder in `glacier` style** (16 September 2026, `bench --bots level:elite:glacier`, paired against the balanced rungs over 768 games a cell): every rung that consults the style gains — `operator` 0.125 → **0.257**, `elite` 0.258 → **0.363** (z 7.2 / 5.0) — so the style is worth as much at `puct@512` as at one ply. But the spacing does not survive it: **`operator → veteran` goes flat** (+0.021, inverted on seed 2) because `veteran`'s `epsilon` 0.10 costs `glacier` 0.086 against `Balanced`'s 0.022. So rungs need per-style calibration, or the top rungs built on `glacier` (§4(b)). The balanced top step is also thin now (+0.022, z 1.0), and §4(a)'s `operator` 0.182 does not reproduce (0.125–0.148), so (a) must re-measure before it interpolates. **§11 took the per-style route**: `glacier`'s `veteran` Corp now plays at `epsilon` **0.02** (`LevelSpec::with_personality`), the measured midpoint of a curve that is flat from 0.10 to 0.05 and climbs only below 0.03 — 0.309 against a target of 0.310, steps +0.052 / +0.055 (z 2.3 each, a rise on each seed alone), so the `glacier` Corp ladder reads 0.012 / 0.092 / 0.257 / 0.309 / 0.363. Other styles still ride `Balanced`'s spacing unmeasured. **§12 measured `rush` (6 of 16 Corp decks) and it has no top**: 0.012 / 0.061 / 0.100 / 0.109 / 0.112 over 768 games a cell, `operator → veteran → elite` flat at +0.009 / +0.003 because `puct@512` as `rush` is worth +0.012 over one ply where as `Balanced` it is worth +0.133 — so a per-style `epsilon` cannot space it. The fix is a decision (repair `rush`, seat its search rungs in another style, or say so on the start screen), not a tuning. **§13 measured `trap`** (2 decks): stronger than `Balanced` at every rung (0.012 / 0.085 / 0.189 / 0.284 / 0.297, +0.039 at `elite`, z 3.3) and its one flat step, `veteran → elite` +0.013, is `Balanced`'s own (+0.022, flat on the same seed) — so no `trap` handicap; §4(a)'s re-spacing of `Balanced`'s `veteran` is the fix and `trap` inherits it. Owed Corp ladder work is now that step and `rush`'s missing top. **§14 re-spaced `Balanced`'s `veteran` Corp** to `epsilon` **0.20** (768 games a cell over six values: 0.236 / 0.219 / 0.176 / 0.180 / 0.146 / 0.111 at 0.10–0.30, steep between 0.15 and 0.18 then flat), so its ladder reads 0.012 / 0.062 / 0.125 / 0.180 / 0.258 with steps +0.055 (z 3.0) and +0.078 (z 3.7) at the top, each a rise on both seeds. **§13's "`trap` inherits it" was wrong**: at 0.20 `trap`'s `operator → veteran` went flat and `rush`'s inverted (0.083 below `operator`'s 0.100 on both seeds), so `trap` carries its own 0.15 (0.232, steps +0.043 / +0.065 pooled, one step flat on one seed at either nearby value — its span is 0.108) and `rush` keeps 0.10, flat rather than inverted. Every Corp style but `Balanced` now has its own `veteran` handicap. **§15 took §12's decision: a `rush` deck's `veteran` and `elite` Corp play `Balanced`** (`LevelSpec::with_personality`). On the six rush decks alone the `rush` ladder had run backwards (`operator` 0.101, `veteran` 0.062, `elite` 0.035); it now reads 0.101 / 0.149 / 0.271 there and 0.100 / 0.180 / 0.258 over all decks, and the styled rungs replay `Balanced`'s games 768 of 768. So `rush` keeps no `veteran` handicap of its own. Nothing on the start screen yet says the top of a rush ladder is not a rush. Owed Corp ladder work: §4(b)–(c). **§16 audited the Runner profiles the same way** (six seeds × 384 against the balanced Corp): `aggressive` was the worst Runner (Corp share 0.188 against balanced's 0.148, z 4.4) because its thinner grip got it flatlined 140 times to balanced's 36; its grip and subroutine knobs back at balanced make it **0.155** (z 5.3), and it falls against every Corp profile while still running 20.7 times a game to balanced's 17.9. Its run weight is its remaining cost against `rush` (−0.041 at 0.6) and is the archetype, so it stays. `cautious` and `wary` measured level with balanced and have nothing to repair. **§17 measured the Runner ladder in all five styles** (1,536 games a cell against the one-ply Corp): every step of every style rises at z ≥ 9.0 and on each of four seeds alone, so no Runner style needs its own handicap — the Runner ladder is one base bot, so a style moves the whole line together. The repair lifted `aggressive`'s `elite` 0.782 → 0.831, level with the calibrated rung. **§18 measured §4(b)'s combined Corp** — `glacier`'s fort plus `trap`'s ambush terms: the strongest one-ply Corp yet, **0.326** against `glacier`'s 0.282 (z 6.7) and +0.039 to +0.053 against every Runner profile, but at `elite` (`puct@512`, 768 games) only **+0.013 (z 1.1)** — search finds the ambush lines itself, as it found §3's rez terms. Nothing ships; the one top-rung lever measured stays `glacier` at `elite`, +0.096 over `balanced` on both seeds. Owed Corp ladder work: which style a non-`glacier` deck's top rungs play, and §4(c). | [phase-5-difficulty-ladder.md](docs/roadmap/phase-5-difficulty-ladder.md) |
| **Phase 6** — Menu-driven client | **§1 done (13 September 2026): `netrunner_cli` with no arguments opens a main menu** — Play vs Computer, Learn to Play, Ratings, Settings — and every game it launches returns to it, the new-game form reopening on the game just played with the rung moved to the new suggestion. One terminal for everything; a game that fails to start is a notice, not a drop to the shell. Settings (player name, format) persist and fill unset flags, never typed ones, and measurements never read them. The flags stay and still skip the menu: the menu is the flag path. **§2 done: a deck builder in the TUI** — Decks lists every deck with its legality, built-in ones read-only and copyable, saved ones edited against the format's card pool with running totals from a new core `DeckTally` (sharing the validator's influence rule) and the validators' verdict; every edit saved as made. It found the validator had no out-of-faction agenda rule (fixed in #19, Rules Audit, last section). **§3 done: network play from the menu** — Play Online hosts a human-vs-human server in-process (the host joins it over loopback and plays a masked view like anyone), joins one by address, or watches; each player brings their own deck on `Connect`, validated at the server's format, and the deck's side is the seat (two Corp decks never pair). Connections run as polled tasks, so the lobby wait draws and Esc leaves the lobby for real. Hot-seat is out of scope: it shows each player the other's hidden cards. **Phase 6 is done**; open threads are small (hosted games unrated, lobby resume, remembered server address). | [phase-6-menu-driven-client.md](docs/roadmap/phase-6-menu-driven-client.md) |
| **Phase 7** — Desktop client | **§1 done (14 September 2026): the foundations.** A Bevy 0.19 client (`netrunner_desktop`) with a main menu, a profile and a settings screen, on a toolkit-agnostic core lifted out of the terminal client (`netrunner_client`: settings, deck store, ratings, format pool) so both clients read and write the same files — the settings struct is shared because the file has no `deny_unknown_fields`, so two structs would drop each other's fields. A card-image cache in `netrunner_card_sync` (`CardImageStore`, NetrunnerDB's `imageUrlTemplate` read off the API and kept in a manifest). Its own CI job on three platforms with Bevy's Linux headers; the engine jobs exclude it. Three licenses added to `deny.toml`, each with the crate that forces it. Found on this box: the discrete GPU enumerates and cannot present to the Wayland surface, so the renderer defaults to the low-power adapter. **§2 done (14 September 2026): card faces and the browser.** A card face at two sizes — the printed layout in text (`netrunner_client::card_face` decides which number sits in which corner, tested per type and over every printing; `card_text` splits the text at its icons), swapped in place for the NetrunnerDB scan once it is cached — and drawn card backs with the official PNGs as a documented drop-in. Noto Sans Symbols 2 added for the icons, since Noto Sans has no `◆ ● ⏱`. The browser: every printing (271, reprints separate, catalog-only cards marked "not implemented"), chips by side, faction, type and format, a live search, an inspector with the numbers line the TUI prints and "Engine reads it as" (`prose` lifted into `netrunner_client`), and the image download with a progress bar, opt-in via Settings. Two bugs found by the first face drawn: Core Set image URLs lost their leading zero (`1001.jpg` for `01001`), and `<li>` items ran together in the printed text. Driven by hand (15 September): the grid did not scroll — taffy measures a grid's scrollable content as one cell, so the grid now sits in a flex column that scrolls — and it gained scrollbars, arrow-key selection, a larger inspector face, drop-downs in place of the chips, set and format filters, a "Legal in" line, and NetrunnerDB's icon font fetched to the cache on the images opt-in for the factions', sets' and symbols' marks. **§3 done (15 September 2026): the play area.** A game is played on a board, against a rung, from a form. `netrunner_client::play::MatchHandle` runs the local `Session` on its own thread behind channels (one message per applied action with the person's masked view and log entry, `Awaiting` when they must act, `Rejected` with the engine's words; `submit` never filters), `board::ActionMap` ties every legal action to the card, install, server or identity a click would mean it by (indices into the one list, so the flat panel stays the contract), and `board::diff::transitions` computes what changed from the masked events and the two views — tested over real games as both viewers, which found that a program can be installed from the heap. The form's state machine and the action labels were lifted out of the terminal client rather than copied, so both clients list, suggest and default the same. The board: strips, the Corp's servers as columns with ICE as flat bars, the rig in rows, the hand, the prompt in the card's own words, the action panel, the log, and overlays for a popup, a card's text, the quit prompt and the end of the game. Two dev hooks (`NETRUNNER_GAME`, `NETRUNNER_AUTOPLAY`) let the board be screenshotted sixty decisions in, which found the person's hand below the fold. **A person then played it** (15 September): every card click did nothing (a face is a `Button` the shared feedback never reported — fixed, with a test), the cropped faces read as broken cards, the cards were too small to tell apart and the numbers too dim to see — all fixed on the branch — and six things noted for later, in the area file: a `Toggle selection of card N` label that names no card, a lone `Pass priority` that should not need a click, an `operator` Runner that ran Archives into damage three times and never drew, a layout that is a list rather than a table, actions that should live on the cards, a HUD, a window that must fit the whole game without scrolling, and **a click to examine a card that installed it** (a card with one action submits on click — the first thing §4 removes). **§4a done (15 September 2026): cards and zones as the way to act.** A click on a card or a zone opens a sheet — the card large with its legal actions, or the zone's actions and contents as the mask allows (Archives face-up for the Runner, everything for the Corp) — and never submits by itself; the basic actions are a fixed control bar per side (`netrunner_client::board::Control`, greyed when not legal), the prompt's decisions sit under the prompt, and the flat panel and the log are the "play helper" and "play history", off by default behind a painted gear (neither bundled font has one) and on the Settings screen. **The board fits the window and never scrolls** (a rule in AGENTS.md §5 now, after a static card size put the hand below the fold a third time): fullscreen, the card width computed from the window and what is on the board, rows that overlap rather than wrap, the control bar centred along the bottom beside the hand. Four more items noted for later (two others, the options window's toggles falling outside it and decisions as a centred pop-up rather than rail buttons, fixed the same day): the official card backs fetched to the cache (done the same day: fetched from jinteki.net's copies on the images opt-in, put under the drawn back's handle when they land), the servers right to left (Archives, R&D, HQ, remotes — done the same day as the table seen from the chair: the Corp's own order, the Runner's mirror, ice out toward the Runner with the outermost nearest), decks drawn as stacks (dropped the same day, no longer wanted), and a right-click menu of a card's actions (done the same day: a secondary click — the right button, or Ctrl with the primary — opens the sheet's list as a menu just above the card, closed by Escape, a click elsewhere or the board moving, never through an overlay). **§4b done (15 September 2026): tile stacks and a paced run.** A server column is one stack of tiles — header, root cards, ice, each a click on its sheet — so no face sits in a column and the board keeps its card width; a run is a trail read off the entry's own events one step at a time (`netrunner_client::board::trail`, because one `ContinueRun` can pass, approach and encounter in a single entry and a `Transition` only names where it ended), released a beat apart by the client's pacer (`DesktopPrefs::animation_speed` finally consumed; 0 is instant) into a lane between the two areas that is always reserved so the cards do not resize — the server, a chip per ice with a dot per subroutine, the access, the outcome, and what the run did beneath, kept until the next run or turn. Every tile says whether it is rezzed (or face down, or how far an agenda is advanced) and its sheet lists the install's facts from one shared module (`board::facts`): place and order met, rez cost, strength now and printed, subroutines with their status, tokens, counters, trash cost, hosting. A line from the lane to the column was drawn and dropped the same day on the person's word; the column's border says which server. **§4c done (15 September 2026): a card-selection prompt names its cards, in both clients.** `Toggle selection of card N` is now `Select Hedge Fund`, `Select Ice Wall (protecting HQ)` when the place is what tells two apart, `Confirm Hedge Fund` and — when one card may be chosen and is — `Select a different card`, with `Selected: …` under the prompt. The view alone could not say it, since an R&D or stack search has no cards in the view, so the engine publishes the prompt's candidates to the chooser only (`ClientView::selection`, an opponent's unrezzed install `None` on the board's own condition), guarded in the fog sweep; identical copies in one place are one button (the person's call), which changes the decisions-cover-every-index rule to allow for them. Engine flow unmoved: the 192-game random-vs-random report is byte-identical to `main`'s. **§4d done (15 September 2026): where a card a card installs may go.** A person's Scatter Field install looked as if it had to overwrite their credit asset. The rules and the engine agree (a new remote is always offered; an agenda or asset installed over one trashes it), but the labels read `Choose Remote(1)` and the desktop had no way to click a server that does not exist yet. Now `Install in a new remote server`, `Install in Remote 0 — trashes Nico Campaign`, `Install protecting HQ — costs 1 credit`, under a prompt that names the card and the rule, as pop-up buttons (`netrunner_client::placement`). A back-out to the card choice was weighed and declined by the person: it needs a new `PlayerAction` and an `ActionSpace` break. No engine change. **§4e done (17 September 2026): a lone pass is taken without a click, in both clients.** `netrunner_client::play::lone_pass` is `Some` exactly when `legal_actions` is one `PassPriority` (96 of 217 decisions in one game from the Runner's chair); the terminal (local and remote) and the desktop submit it on arrival, the desktop behind the run pacer's beat, a rejected pass back on the bar, never in a lesson. A client policy: no engine, session or `MatchHandle` change. **§4f done (17 September 2026): a HUD.** Each side's strip holds a grid of large numbers in fixed places (`netrunner_client::board::hud`): Credits, Clicks, Points and the hand in the same slots for both sides, then Bad publicity, or Tags and Damage, shown at zero and drawn in the danger colour when live; inside the strip's old height, so no card size moved. The Agendas readout opens the side's score area as a list whose rows expand in place to the card, its points and its text. **§4g done (17 September 2026): a click opens a card's actions, a secondary click reads it.** The primary click opens the menu above the card, and the right button (or Ctrl or Cmd with the primary) opens a sheet with no actions: the card alone, an install beside its state, a zone's contents. Four more noted, one PR each: keyboard shortcuts, a phase bar toggled with L, dragging to reorder the hand, and dragging a card to play it with its destinations lit. **§4h done (17 September 2026): keys.** Space passes or continues, Enter ends the turn (a second Enter while clicks are left, because the engine rightly lists `EndTurn` with clicks unspent, which corrects the recorded design), C/D/R/P/J/A are the bar, 1–9 the menu's or pop-up's buttons, M/I the hovered card's two clicks, Tab the score area, H the play helper, and `?` or F1 the list. Each goes through its button's intent, so the engine's list decides. **§4i done (17 September 2026): one server order.** Archives, R&D, HQ, then the remotes, from both chairs and in both clients (`board::table_servers`; the TUI lists every central with its count), so the centrals never move as remotes are made; the Runner's mirrored row is gone. The Corp HUD dropped its HQ readout, which repeated the column header. **§4j done (17 September 2026): a phase bar.** The turn's steps — draw, actions with the clicks left, discard — and a run's four beside them, every step always listed with the one in play marked (`netrunner_client::board::phase`), a line naming who holds priority in an open paid-ability window, on by default and toggled with L or the gear. It is a row of the board, so the cards shrink by its height and nothing scrolls. **§4k done (17 September 2026): a hand card is dragged into place.** A hand card's press is armed rather than acted on, so a still release is the click that opens its menu and a travelled one drops the card into the person's own order for their hand (`models::game::HandOrder`, kept in the client, never sent: the rules give a hand no order). **§4l done (17 September 2026): a card dragged onto a lit place is played there.** Holding a hand card lights the places its own entries name — a server, an ice host, and a column a new remote gets for the length of the drag — and a drop submits the entry that lands there, or opens a menu when a place offers two. A drag is the one gesture on the board that submits; a click still never does. That completes the person's second list (§4g–§4l). **The person's third list is noted (17 September 2026) and is next**, from three MTG Arena screenshots and one sentence — the board is correct and boring: depth in the play area, an avatar and a lit/muted bar per side, a grip that arcs with the considered card lifted (local to whoever looks; considering a card is not a game event), the space used including a turn indicator, the right side carrying the card being read, and a background for the field of play with several to pick from or one at random. It closes §3's item 4, the last of the first list still open, minus the mirrored side §4i already killed. **Two decisions before it starts.** *No 3D, and the first reason given for that was wrong*: `ui` already pulls the whole render stack, so real perspective costs zero to one new crate, not hundreds — the true cost is ~1,000–1,500 lines (the board proper is 576 draw lines, `models/` has no `bevy` in it at all, but 16 of 17 board tests are `bevy_ui`-shaped and `clicks_in_tree_order` *defines* §4i's server order as tree order). Deferred, not refused: what 3D buys is motion in perspective, so revisit it when a run should feel dramatic. *The depth is painted, not computed* — a per-row width ramp was rejected because the natural form is **not monotone** in the face width and `face_width`'s binary search is licensed only by monotonicity, so it would have returned a silently wrong width with every test still green; the safe affine form is recorded for if it is ever wanted. After that list: the transitions as movement and sound. **§4m done (17 September 2026): a table under the board.** The board is played on a field, and the field is where the depth is — every card stays one size, so the perspective is painted rather than computed. A table is a folder under the three asset tiers (a JPEG ground, an optional alpha overlay, an optional `table.json`), selectable or random per match, with a painted receding grid as the tier that needs no files; `NETRUNNER_TABLE_GUIDE=1` draws the board's real rows over it so a field can be painted to the layout rather than guessed at. **§4n done (17 September 2026): the furniture is drawn instead of outlined.** Archives, R&D, HQ and the remotes were a colour, a border and a radius; they now take artist-made art through `bevy_ui`'s nine-slice, 33 slots with per-state variants, an undrawn slot keeping its outline and an undrawn state borrowing its base's picture — so one PNG is a usable skin. **A skin changes how a box is painted, never how big it is**, measured: the control bar's buttons are identical skinned and undressed. A table may name the skin it was drawn for. Server marks are deliberately not slots, because a mark widens its header and that is a layout change. **§4o done (17 September 2026): an access shows the card, not its name, in both clients.** The person could not tell what they were being asked to steal or trash: the desktop printed `Accessing Send a Message` and a trash cost, the terminal printed nothing at all (an access is not a `PendingDecision`, so the pane kept its default title). The accessed card's `CardId` was already on the wire, masked, so the new `netrunner_client::access` needed no engine change — the desktop pop-up carries the large face above its steal/trash/pass buttons, and the terminal draws a panel over the board with the actions pane keeping its keys below. **The pop-up is the second exception to "a reading surface carries no actions"**, after the score area, and for the same reason: an accessed card is in HQ or R&D with no tile to click, so the prompt is the only place it exists. Only the side being asked sees it. `Face::numbers_line`/`Face::lines` folded three hand-rolled copies of the printed-numbers line into one and dropped the `Cost 0` all three printed on agendas and identities. **§4p done (18 September 2026): the board says what can act.** Not from any list — the person asked for it on hearing what was next, having expected to find it there, and it is the debt §4g opened: once a click stopped submitting and started opening a menu, nothing said which cards had a menu worth opening. Every legal action takes one of two moods (`netrunner_client::board::affordance`) — purple for a move at the person's own pace, yellow for a moment that will pass (a run, an open window, a parked prompt, a pop-up's own buttons) — and the classification is **per action, not per moment**, exhaustively, so a new `PlayerAction` does not compile until someone says which mood it is; the two actions that are genuinely both (`RezIce`, `ActivateAbility`) take the moment as a tie-break. A `BoxShadow` rather than an `Outline`, because `Outline` is already spoken for three times on this board and a node has one — and because the contact shadows still owed can be a second entry in the same component. **Gated on `awaiting`, not on the map being empty**, which the test forced: a seat off priority keeps legal actions of its own (the Corp's standing rez, 57 times over six games), so the side-agnostic `legal_actions` the Session Rule relies on is exactly why the gate is the client's. In both clients; no engine change. **§4q done (18 September 2026): the cards sit on the table.** The third list's item 1 is closed — §4m painted the perspective into the field, §4q puts the cards on it with a contact shadow per row (`models::layout::Depth`, the four rows of `spawn_board` top to bottom, each `(y, blur, alpha)` growing toward the chair, a tile raised one row off its column). **This is the depth ramp the list rejected, in the one place it is safe**: a face-width ramp is not monotone and would have broken `face_width`'s binary search silently, while a shadow is paint and is measured by nothing. The glow had already spent the node's one `BoxShadow`, so `Contact(Depth)` and `Glowing(Affordance)` declare intent and a single system composes the vector — two inserts would have meant whichever ran second silently won, losing either the depth or the affordances by system order. Held until after §4n on purpose, and it consumes the `Manifest::shadows` / `Skin::wants_shadows` hook §4n left: a skin whose pictures carry baked shadows turns the drawn ones off. Chrome casts nothing. **§4r done (18 September 2026): the sheet is the card.** Not from any list — the person looked at a card's sheet and named the name over it as redundant, the Close under it as unnecessary against Escape, and the plain box as wanting art. The card prints its own name in both tiers (twice, on the text face), and Escape already closed the sheet, so both went and a click that misses the panel became the second door; a *form* keeps its Close, because a setting lost to a stray click is worse than a button nobody needed, and the three panels that are asking a question stay modal. The card browser's inspector took the same cut, which drew the title *below* the face. Four skin slots follow (`panel` and its `sheet`/`decision`/`menu` states, plus `overlay.scrim`), which is the half of "the panels, menus and icons" §4n left owed. **The item found a live bug on `main`:** `Node` requires `FocusPolicy` and defaults it to `Pass`, so every overlay's wash let presses through to the control bar behind it — a click through an open sheet onto "End turn" ended the turn — and `Button`'s required `Block` does not fix it, because a required component is inserted with `Keep` and the spawn has already supplied `Pass`. Found by a test written to assert the opposite of what the code's comment claimed. **§4s (18 September): the table has a near side and a far side, and its middle holds still** — the opponent's side at three quarters of the person's card width (one constant, so the fit's search stays valid), both hands a peek of a third with the hovered card lifting out whole, the control bar above the hand, and the card width no longer reading what is installed: the rig's row is reserved and the ICE grows into a flexible field between the server plates — a 16:9 box reserved on every server for its picture — and the run lane. **§4t–§4u:** every plate a building and every tile its kind (hatched face-down, bricks, gates, rings, coins, washed in the state colour), counters as their kind's badge and the HUD with Null Signal Games' own glyphs (CC BY-ND 4.0, credited in `assets/CREDITS.md`), all drawn by default and listed with their sizes in `assets/README.md`. **§4v:** every committed asset is a separately licensed work with a row in `assets/CREDITS.md` (owner, website, licence, changes) — project-made (including with AI) is GPL-3.0-or-later, anyone else's keeps its owner's licence — enforced by `tests/credits.rs`. **§4w:** card scans are NetrunnerDB's 750 × 1050 WebP wherever it has one — every System Gateway and Elevation card; NSG's PnP PDFs were measured at the same density and not used — with the cards left at 300 px listed (`CardImageStore::low_res`, `netrunner_cli cards images`), and each face drawn from a copy resampled to the width it covers. **§4x:** a choice between cards shows the cards — a selection draws each candidate as its own button, sized to fit the window (`layout::choice_faces`), and an install or a text choice shows the card it is about (`board::Prompt::card`), in both clients. **§4y:** every place has an asset slot before its art exists. There is a splash (skippable, 1.5 s). Every non-board screen has its own backdrop slot keyed off `AppScreen` (a new screen will not compile without one, and a test fails until its guide row exists) and falls back to a shared `menu.jpg`. The Corp's board art can be styled per faction (`board/corp/<faction>/`), chosen from the Corp identity. The default table is Random over the shipped tables, never repeating, and the drawn tier is only the fallback and Settings → Basic graphics, never in the rotation. Next: the panels' art, the remaining icons, and wiring `"tint": "state"`, which the skins guide promises and nothing implements. **§4z:** the board runs the window's full height, with the hands on its top and bottom edges. The status line, the phase (now a panel, which moves no card when hidden) and, during a run, the Runner's identity cropped to its art are all in the right column. At 1366×768 the cards grow from the 72 px floor to 119–134 px. **§4aa:** the rig is three rows (programs, hardware, resources), with programs next to the ICE from either chair. Each row shows the top third of its cards, so no card shrank, and a click on the heap lists every card in it. **§4ab:** mid-encounter, every card that can break the whole of the ICE is one button with its price ("Break Wall of Static with Corroder · 2 credits"), in all three clients. The route is found by playing each card's abilities on a determinized sample through `apply_action`, because the view's breaker strength leaves out Echelon's and Rising Tide's modifiers. It is carried out one legal action per view, and stops if the price rises. **§4ac:** the state of an encounter is marked where it is decided — the ICE being met, its strength, and a line per subroutine `[x]` broken, `[!]` fired or `[ ]` pending, above the routes in the desktop's right column and under the ICE in the terminal's run block, struck through there. The words come off the run's own `SubroutineDef` rather than the registry, so an added subroutine is listed and a removed one is not, and `mask_run_ice` is the only gate needed. One vocabulary with the sheet (`subroutine_word`), tested to agree over real games. A pip per subroutine on the tile was rejected: the run lane already draws exactly that, and it cannot say *which* subroutine. `NETRUNNER_HOLD_ICE` holds the autoplay on a settled encounter, which is what §4ab's unshot rail needed. **§4ad:** a menu and a pop-up never put an option off the window. The actions menu placed itself from an estimate of its own height — 40 px a row, when a row whose label wraps is 57 — and the branch that flipped it below a card clamped on one side only, so a tall menu lost options off the window, where the screen root's clip ate them. It is now pinned by the edge that faces the card and grows away from it (`models::layout::menu_box`), so its height is never needed, and its entries wrap into a second column when the room on that side runs out. Measuring it after the layout ran was rejected (a frame late by construction, so a flicker on the most-used click, and nothing is measured in the headless tests), and a scrolling list was asked about and turned down: a scroll bar makes an option reachable, not visible. The decision pop-up, which guessed its chrome the same way, is capped at the window with its **cards** shrinking first and never a button. Two dev hooks were needed to see any of it (`NETRUNNER_MENU=most|top`, `NETRUNNER_WINDOW=WxH`), because the old hook could only open a one-entry menu on the window's bottom edge. **§4ae:** a card says what it will ask before it is played — a report from play: Red Team spends its click and only then shows which servers are left. The button now reads "Use Red Team — then asks: Run on HQ / Run on R&D" (`netrunner_client::board::preview`): the action applied to a determinized sample with the engine's own `apply_action`, as `board::breaks` prices a route, and only for a choice made from public information — a server or a card's printed options, never a selection of cards, and never past a moved `rng_step`. Reading `allowed_servers` off the JSON was rejected as a second legality check in a client. Once per view, never per frame. **§4af:** a move can be taken back — `Session::with_undo` / `rewind`, a restore in the session and not a `PlayerAction`, so `ActionSpace` stays 1646 and §4d's declined install back-out comes with it. **Free** while the person is still on the prompt their own move opened and it taught them nothing (`rng_step` unmoved, no `GameEvent::may_teach_the_actor`, the other seat silent, no prompt over a hidden zone), which leaves a rated game rated; past that it is an **undo**, four moves deep inside one turn, which goes back just as exactly and drops the rating unrecorded. jinteki's undo is online, unilateral and has no such test. The history is truncated with the state, so the record still replays; 192 random-vs-random games byte-identical against `main`. Local games only, the rule in the session so the server can take the free half later. **§8 (open, renumbered from §5 on 19 September 2026 — §5–§7 are the deck builder, lessons and replay, and online):** eleven client features borrowed from jinteki.net; the three-row rig, auto-pump-and-break and the marked subroutines are done. | [phase-7-desktop-client.md](docs/roadmap/phase-7-desktop-client.md) |
| **Infrastructure** — CI | **Narrowed 14 September 2026: the merge gate is the Linux tests and the security checks only** — `test` on ubuntu (clippy and test, desktop excluded), `deny`, `workflows` and `secrets` under the required `ci` aggregate. The Windows and macOS legs, the `desktop` job, `beta`, `docs`, `features` and `machete` moved to `platforms.yml`, weekly and on `workflow_dispatch`, gating nothing: the three-OS matrix (the desktop job alone 33m35s cold on Windows) was what every merge waited on, and a platform break is worth hearing about within a week rather than worth holding every merge for. The desktop crate is checked by the local `cargo test --workspace`. What follows records the pipeline as first built, on every push. **Done (11 September 2026).** GitHub Actions on every push and PR: `cargo test --workspace` and `cargo clippy --workspace --all-targets` on **Linux, Windows and macOS** — the first time this workspace has been built anywhere but one Linux box — plus `cargo doc` with warnings denied, each optional feature one crate at a time (`fs-loader`, `onnx` per crate, `netrunner_gym`'s `extension-module` as a check, since with it PyO3 links no `libpython` and no test binary can run), `cargo-deny` over advisories/licenses/bans/sources, `cargo-machete`, `actionlint`, `zizmor` and `gitleaks`. Tests build with `CARGO_PROFILE_DEV_OPT_LEVEL=2`: the sweeps are search-heavy and opt-level 0 dominates the suite, while release would drop `debug_assertions` and the integer-overflow panics that guard click/credit/counter arithmetic — `opt-level` is independent of both. The two 256-seed sweeps run weekly and on `workflow_dispatch`; **they remain a manual pre-merge step**, because a weekly job is not a gate. **The pipeline paid for itself before it ran**, in four findings: `cargo-machete` found `netrunner_gym` carrying `serde` and `serde_json` with no use site anywhere in the crate; `zizmor` rejected tag-pinned actions outright at high severity, so every `uses:` is pinned to a commit SHA with the tag in a trailing comment and Dependabot (with a `cooldown`, so no release is adopted the day it ships) is what moves them; and `cargo doc -D warnings` found seven public doc comments linking to deliberately private items (`enforce_limit`, `finish_end_turn`, `enter_start_of_turn`, `agenda_point_range`, `MAX_COPIES_PER_CARD`), now code spans rather than links that could never resolve. That same run forced one decision: rustdoc reads Netrunner's printed symbols — `[click]`, `[c]`, `[credit]`, `[mu]`, `[trash]`, quoted verbatim from card text as a matter of house style — as intra-doc links, 42 of them across the DSL modules. `broken_intra_doc_links` is therefore the one rustdoc lint allowed, at the CI flag rather than in source so it is visible in one place; every other rustdoc lint stays denied. The alternative, escaping each as `\[click\]` in perpetuity, taxes the most distinctive habit in these comments to protect rendered docs this project does not publish — worth revisiting if it ever does. `libpython` is the one fragile part — `netrunner_gym`'s `pyo3` is not optional, so every `--workspace` build on all three platforms needs an interpreter *and* `LIBDIR` on the loader path, which is why the setup is a composite action rather than copied into four jobs. Two deliberate omissions, both recorded in `AGENTS.md`: no `cargo fmt --check` (~3,860 diff hunks, and not a width setting — 2,088 remain at `max_width = 200`; gating it means a mass-reformat commit that buries every `git blame`), and no `--all-features` anywhere. The fourth finding came from the macOS leg on the third run, in a commit that touched only a workflow timeout, so it was latent rather than introduced: two `netrunner_card_sync` tests could share a temp directory and delete each other's cache file mid-write. `tempfile_dir::TempDir` named itself from `process::id()` plus `SystemTime::now().as_nanos()`, and neither half is unique across parallel threads of one test binary once the clock is coarser than a nanosecond — which Linux's is not and macOS's is, so the sweep had been green for as long as the helper existed. Now a process-wide `AtomicU64`, with a tight-loop test that fails wherever the clock is coarse, because 40 green local runs proved nothing. **All three platforms passed on the first run** — no iteration, and the `libpython` wiring held everywhere: cold-cache `test` at 4m26s ubuntu / 8m13s macos / 9m43s windows, `features` 3m59s, `beta` 5m16s, everything else under 40s; job timeouts are sized on those. The deep sweep cannot be `workflow_dispatch`ed until this is on `main` (that trigger only reaches workflows already on the default branch, and the same is true of both crons), so its commands were rehearsed locally instead: at 32 seeds in release the two sweeps cost 7.8s and 4.8s, putting 256 seeds around a minute each plus the build. **The `ci` aggregate check is now required on `main`** (`strict` off so a solo PR needs no rebase on every main move, no review requirement, `enforce_admins` off as an escape hatch), which also blocks force-pushes and deletions there — the "never force-push `main`" rule is enforced rather than documented. Dependabot alerts and automatic security updates were **disabled** and are now on; `dependabot.yml` never covered that, since it configures *version* updates only, though `cargo-deny advisories` reads the same RustSec database on every PR, so what was missing was GitHub-native notification rather than detection (0 alerts today, agreeing with cargo-deny). zizmor runs at `--min-severity low` and uploads SARIF to the Security tab. Its last five findings — local actions referenced as `./`, which resolves against the runner's filesystem, so an earlier step could swap the action's contents — are fixed by GitHub's `$/` self-repository form. **That fix required an escape hatch, and it is the one thing here a future reader will want to delete without knowing why**: actionlint 1.7.12 does not know `$/` and rejects all five as malformed refs, so its step carries `-ignore \$/\.github` — space- and quote-free because that action's `flags` tokenizer honours double quotes only and turned an earlier, quoted pattern into four arguments. It stays narrow (a genuinely ref-less `actions/checkout` still errors) and should go when actionlint learns the syntax. | `.github/workflows/`, `deny.toml` |

## What is next, in order

**The player-facing series was built and merged on 13 September 2026** (#11–#16): deck styles that a rung keeps, two new Runner styles (and the measured absence of a fourth Corp one), a rating for local play with a suggested rung, the daemon seating a rung, a start screen, and the player's own hand drawn. None touches the rules; the random-vs-random report is byte-identical throughout. **A person then played it, and the verdict was that it is driven too much by switches** — which is Phase 6:

1. **Phase 7, the desktop client**, one PR a phase (§1 foundations, §2 card faces and the browser, and §3 the play area — the `MatchHandle`, the `ActionMap` and the view-diff `Transition`s — are built): §4 feel — done first (§4a): a click never submits by itself and the actions live on the cards and the zones, behind a control bar and a gear menu; the official card backs are cached on the images opt-in, and the board is the table seen from the chair (servers and ice in the rules' order); and a secondary click opens a card's or a zone's actions as a menu above it (decks as stacks was dropped); §4b made a server column one stack of tiles and a run a paced trail in a lane of its own; §4e took a lone `Pass priority` without a click in both clients; §4f put each side's numbers in a HUD of fixed places; §4g made the primary click open a card's actions and the secondary click read it (the card, an install's state, a zone's contents), the first of the person's second list; §4h put the bar's and the pop-up's buttons on keys (Enter asks twice with clicks left) with a list on `?` — §4h put the bar's and the pop-up's buttons on keys (Enter asks twice with clicks left) with a list on `?`; §4i–§4l finished that list — one server order from both chairs, a phase bar, and a hand card dragged to reorder it or onto a lit place to play it; **the person's third list (noted 17 September 2026) is how the board looks**, and its first two items are built: §4m put a table under the board carrying the depth (no 3D — costed, deferred, and the first reason given for rejecting it was wrong), and §4n made every tile, header, button and chip take artist-made art through nine-slice without letting it change a size; §4o then answered a report from play rather than the list — an access shows the card instead of only its name, in both clients, with its actions under it (the second exception to "a reading surface carries no actions", after the score area, because an accessed card has no tile to click); §4p answered a second ask from outside the lists — the board now glows what the engine will accept, purple for a move at the person's own pace and yellow for a moment that will pass, in both clients, which is the debt §4g opened; §4q then closed the third list's item 1 with the contact shadows that sit the cards on the field, sharing the glow's one `BoxShadow`; still owed from that list are the panels/menus/icons and a server's own mark, the fanned hand and the hovered card, an avatar and a lit/muted bar per side, and the right side earning its width; then the other transitions turned into movement and sound; §5 the deck builder; §6 lessons and replay; §7 online. **The running queue is now §8, the eleven features borrowed from jinteki.net** (renumbered from §5 on 19 September 2026, which had collided with the deck builder's address): items 1–3 are done — the three-row rig, auto-pump-and-break, and the marked subroutines — item 4 — a card says what it will ask, and a move can be taken back — is done (§4ae, §4af), and item 5, a replay viewer over `MatchHistory`, is next in that queue. **The work that interrupted that queue is done** (20 September 2026): Rules Audit backlog item 1 — one event queue, one audience rule, one checkpoint — was taken first because it is the one engine gap that gets more expensive with every card, and landed as five PRs (#91–#95); its stages and its measurement script are in the area file. The order and the reasons are in the area file's header.
2. **A person playing Phase 6**: the menu, the deck builder and online play are each driven by hand in tmux, but not yet by someone playing a real game end to end, or online across two machines.
3. **The Corp chair has no top rung** (15 September 2026, Phase 5 §2): the ladder calibration ran at 768 games a cell, both chairs climb at every step, and the finding it was not looking for is that `level:elite` as Corp wins **0.266** against the un-handicapped one-ply Runner — 0.714 before the Runner's evaluator changed. A Runner-seated player runs out of ladder at rung 5, which is the Runner chair's old cap transferred, and the lever is the Corp evaluator rather than search budget (Phase 2 §5 item 34: it saturates from 512 simulations on). A fourth Corp style needs a card-reading lever the one-ply evaluator does not have. **Revisit the Corp rung spacing, how its top rungs are built, and whether the bots can play in phases at all — Phase 5 §4, opened 16 September 2026; §5 is the first work against it and is the instrument, not the change.** The last of those is the largest: `evaluate_state_with` is a static function of the position, scoring a board the same on turn 1 and turn 20, so the Corp interleaves building and scoring every turn (13.0 installs and 6.3 advancements over 11.4 turns) where a person banks economy and builds the wall and *then* pushes an agenda behind it. The archetypes already name the two halves — `Glacier` "build the fort first", `Rush` "the agenda goes on the table first" — as static whole-game profiles that cannot be sequenced, and balanced beats both (0.198 against 0.174 and 0.109), which is what a pair of half-game strategies would look like. The deck half is already wired (`Personality::for_deck`, and an unset `--corp-personality` *is* the deck's own style); what is fixed for the whole game is the strategy. `GameState::turn` makes a schedule implementable, but `Trap`'s retune is the warning against it — that profile only worked once it was terms that read the cards rather than generic dials — so the first attempt should be terms that move with the board (agenda points still needed, credits against the rez costs already face-down, whether a scoring remote exists) with the turn counter as the fallback to beat. **First cut taken 16 September 2026** (Phase 5 §3, `feat/corp-reads-the-rig`): the Corp now prices ICE by what it stops rather than what it cost (Pharos rezzed 0.26 → 0.52), worth +0.017 to +0.033 at 768 games depending on the style mode — a rise on all eight seed-pairings but under the significance bar on either alone. **It also compressed the Corp ladder, which is the more useful finding**: a better leaf lifts `operator` (one ply, +0.016) and not `veteran` (`puct@512`, +0.000), so the `operator → veteran` step fell +0.055 → +0.039 and reads `flat` on one seed of two where both were `rise` before. Search saturation (Phase 2 §5 item 34) seen from the ladder: lookahead already finds the rez decision the leaf term encodes. **Phase 5 §4 is the owed follow-up, in two parts and in this order**: (b) reconsider how the Corp's top two rungs are built, because `veteran`/`elite` are `puct@512` over the same leaf and a better leaf buys them +0.000 — search budget and evaluator quality are the same lever twice and both are spent, so the measured candidate is a *card-reading* one (`trap` at 0.259 against balanced's 0.204) rather than deeper search; then (a) re-space the Corp rungs, where `operator` is the rung out of place (0.182 against an even-step target of 0.144) and wants a handicap of its own, with its own measured `epsilon` curve because this chair's is visibly not the Runner's near-linear one. Doing (a) first would measure a spacing around a top rung about to be rebuilt — the mistake §2's first Runner calibration already made once. The ceiling is still open — the measured next lever is the ambush terms the balanced Corp switches off by design (`trap` 0.259 against balanced 0.204, chair-isolated). **The `operator` Runner ran Archives three times into known damage and never drew a card** (a person's first desktop game, 15 September 2026, Phase 7 §3) — **fixed the same day** (Phase 2 §5a, reopened): a run is now worth what the breach can find and a card in grip half of what installing it would be. Over 96 heuristic games the Runner's runs fell 3,400 → 1,831 (Archives 627 → 71), draws rose 126 → 332, trashes 12 → 82, and its wins 45 → 81. That made the one-ply Runner the strongest Runner in the workspace, and the Runner ladder's top two rungs are re-seated on it (Phase 5 §2); the calibration above is now overdue rather than optional. Still open from that game: the economy-blind install (nothing costing 3+ is ever installed), the next Runner term.
4. **Server-tracked rating, stage (a) first** (Phase 4 §5, design recorded 20 September 2026): the server keeps the match record it now throws away — no protocol change — then keypair identity, then signed receipts. Nothing local is rated any more (Phase 3 §2), so until stage (c) the only rating anywhere is a daemon's, under an unverified name.

**Parked behind that** — the search research, kept as it stood so nothing is lost. **The Runner chair's part resumed on 13 September 2026** (Phase 2 §5 items 42–43), because Phase 5's Runner rungs are capped by it:

1. **The Runner chair's next lever is the playout policy, not a bigger search** (Phase 2 §5 items 38–43). The leaf route closed first — the leaf was blind (36), reading the sampled ICE was worth nothing (38), and reading it *correctly* was worth nothing either (40) — and item 43 then closed the search route: against a fixed one-ply Corp, `mcts`'s Runner plateaus at **0.60–0.64** across 4–32 samples, 128–1,024 simulations and 16–32-ply playouts, every cell within the 0.037 its own reference moved on a re-roll. That also retires what this line used to recommend, **making PUCT marginalize over hidden state that varies**: `mcts` already is such a search and it sits on the plateau, so the most that change could buy is a PUCT Runner as strong as the `mcts` one the ladder already seats. What every plateau cell shares is `mcts::action_weight` — seven weights playing *both* chairs through every playout — and the static leaf. Playout depth peaks at 16 plies and falls on either side (0.440 at 8, 0.487 at 48), which says the playout's moves cost more than its reach buys; a playout in which the Corp plays like a Corp (the one-ply heuristic's choice for the first plies, say) is the experiment that tests it, now affordable after item 42. It changes search results, so it takes the strength bar.

2. **Re-take `mcts`'s chair numbers at a pinned tree count, if any of them are load-bearing** (Phase 2 §5 item 41). The tree count is now `DEFAULT_TREES = 4` rather than `rayon::current_num_threads().clamp(1, 4)`, and on this box that is a no-op — `--threads 18` is game-for-game identical before and after, so nothing recorded here moves. What it does mean is that any `mcts` number taken *elsewhere*, or with `--threads` set below 4, was a different bot: 52 of 192 games differ between `--threads 2` and `--threads 18` on the old binary, and the Runner chair moves 0.230. Nothing in the roadmap is known to be in that category; this entry exists so that if one turns up, the reason is on the record.

3. **Deeper batching, if a run ever wants it** (Phase 2 §5 item 27). Cross-game batching landed at 2.6× with a byte-identical corpus, and the measured ceiling is higher still — 4 runners at batch 32 reach 46,229 rows/s against the ~11,300 the loop now achieves — but reaching it needs more rows outstanding than ~64 game threads produce. Leaf parallelization inside a search (virtual loss) would supply them and **does** change search results, so it needs the full strength bar. Not worth it until a run is actually waiting on compute.

4. **Whether the pool is Corp-favoured at all, once the Runner chair is fixed** (Phase 2 §5 item 34). 0.703 is an upper bound measured with a Runner we can already beat, and the honest reading of the curve is that it prices *this search*, not the decks. Re-run the budget curve after item 1 lands; if the Corp share falls with a better Runner, there is no balance problem to answer. `scripts/corp_share_curve.py` and `bench` self-pairings are the whole apparatus — 10 s a heuristic leg, 6 min a `puct@128` leg — so this is a re-measurement, not a project.

5. Small open items, each recorded in its area file (an earlier version of this line also listed the `ci` check as not yet required on `main`; it is required — see the Infrastructure row): `netrunner_gym` can still toggle-loop; the coverage card gate is inert at default seeds for decks the sweep has not played eight times; `netrunner_single_player/tests/common/mod.rs` still carries a filler fixture; Core Set cards have no sweep coverage.

## How this file is kept

- Status lives here or in an area roadmap, never in `AGENTS.md`. A new entry goes in the area file under its numbered section; then update the table row and the "next" list here.
- An entry records the decision, the alternative rejected, and the load-bearing numbers (before → after, seeds, game counts, branch). Narrative is compressed at the next pass; numbers and decisions are not.
- A later entry that corrects an earlier one says so in both places rather than deleting the earlier text.
